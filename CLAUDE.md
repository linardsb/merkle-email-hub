# CLAUDE.md

## Project Overview

Centralised email platform with AI agents. FastAPI backend, Next.js 16 frontend, PostgreSQL + Redis. Python 3.12+, strict MyPy + Pyright. **Vertical slice architecture** — features under `app/{feature}/`.

## Essential Commands

```bash
make dev             # Backend (:8891) + frontend (:3000)
make check           # All checks (lint + types + tests + security)
make test            # Backend unit tests
make lint            # Format + lint (ruff)
make types           # mypy + pyright
make check-fe        # Frontend type-check + unit tests
make db-migrate      # Run migrations
make eval-full       # Full eval pipeline (requires LLM)
make eval-check      # Eval gate (analysis + regression)
make eval-golden     # CI golden test (deterministic, no LLM)
make eval-qa-coverage # Deterministic micro-judges coverage
```

## Development Guidelines

**Feature file order:** schemas → models → repository → service → exceptions → routes → tests

**Layers:** Routes (thin HTTP) → Service (business logic) → Repository (DB only). Exceptions inherit `AppError`.

**Key imports:** `get_logger` from `app.core.logging`, `get_db` from `app.core.database`, `TimestampMixin` from `app.shared.models`, `escape_like` from `app.shared.utils`. Roles: admin, developer, viewer.

**Config:** Nested Pydantic settings, `env_nested_delimiter="__"` (e.g. `DATABASE__URL`, `AI__PROVIDER`).

## Architecture

Backend: `app/` (VSA features) — `core/`, `shared/`, `auth/`, `ai/` (agents + blueprints + evals), `knowledge/`, `qa_engine/` (11 core checks + optional resilience check), `projects/`, `email_engine/`, `components/`, `connectors/`, `approval/`, `personas/`, `memory/`, `rendering/`, `design_sync/`, `streaming/`. Frontend: `cms/`. Sidecars: `services/maizzle-builder/`, `services/mock-esp/` (mock ESP APIs, port 3002). Migrations: `alembic/`.

## Roadmap

See `TODO.md` for details. Completed: phases 0-18.5, 19.1, 19.3 (core platform through Rendering Resilience Testing + Outlook dependency analyzer + Lightning CSS Email Compiler). See `docs/TODO-completed.md` for detailed completion records.

**Upcoming phases (priority order — highest differentiation first):**
- **Phase 19.2, 19.4–19.5** — Outlook Transition Advisor completion (audience-aware migration planner, frontend dashboard, tests)
- **Phase 20** — Gmail AI Intelligence & Deliverability (Gemini summary prediction, schema.org auto-markup, deliverability scoring, BIMI)
- **Phase 21** — Real-Time Ontology Sync & Competitive Intelligence (caniemail auto-sync, rendering change detection)
- **Phase 22** — AI Evolution Infrastructure (capability registry, prompt store, token budget, fallback chains, cost governor)
- **Phase 23** — Multimodal Protocol & MCP Agent Interface
- **Phase 24** — Real-Time Collaboration & Visual Builder
- **Phase 25** — Platform Ecosystem & Advanced Integrations (plugins, Tolgee, Kestra, Penpot, Typst)

**Completed:** **11.25** client design system & template customisation — all 5 subtasks done (11.25.1 design system model, 11.25.2 component→section bridge, 11.25.3 project-scoped template registry, 11.25.4 agent pipeline constraint injection — design system as generation constraints with role-based token mapping + locked fills + brand color sweep, 11.25.5 consistency enforcement — `BrandRepair` stage 8 in repair pipeline + e2e brand enforcement tests). **12.1** protocol extension + Figma API integration (file structure, components, image export — 3 new protocol methods + Figma implementation + schemas + routes). **12.2** asset storage pipeline (`DesignAssetService` — download, resize ≤1200px, local storage, authenticated serving with CSP headers, cascade delete on connection removal). **12.3** design import models & migration (`DesignImport` + `DesignImportAsset` models, 6-state workflow, nullable `result_template_id` FK, repository CRUD, Alembic migration, Pydantic schemas). **12.4** layout analyzer & brief generator (`LayoutAnalyzer` pure-function section detection + `BriefGenerator` markdown output, 2 new endpoints: `POST /analyze-layout`, `POST /generate-brief`). **12.5** AI-assisted conversion pipeline (`DesignImportService` orchestrator with own DB session via `get_db_context`, two-phase UX: create import with brief → trigger conversion, `DesignContextSchema` for Scaffolder with image URLs + layout + tokens, `build_design_context_section()` prompt injection, 4 new endpoints: `POST /imports`, `GET /imports/{id}`, `PATCH /imports/{id}/brief`, `POST /imports/{id}/convert` 202 Accepted, atomic Template+TemplateVersion creation, state machine validation, 24 tests). **12.6** component extraction (`ComponentExtractor` — Figma components → Hub Component + ComponentVersion with Scaffolder-generated HTML, category detection, duplicate handling via Figma origin in `compatibility` JSON, background job via `asyncio.create_task`, `POST /connections/{id}/extract-components` 202 Accepted). **12.7** frontend file browser & import dialog (recursive `DesignFileBrowser` tree with thumbnails + checkbox selection, tabbed `DesignImportDialog` with 4-step Import Design wizard and 3-step Extract Components wizard, 8 new SWR hooks, `/figma` consolidated into `/design-sync`, 44 i18n keys across 6 locales). **12.8** design reference panel in workspace (right sidebar with `DesignReferencePanel` — auto-detect design import by template via `GET /imports/by-template/{id}`, manual connection picker fallback, `AssetViewer` image gallery, `TokenDisplay` with 10 token interactions: insert-at-cursor, find-and-highlight, replace-in-selection, copy-as-formats context menu, usage heatmap badges, drag-and-drop to editor, off-brand color auto-correct, CSS variables generator, hover spotlight, Tailwind utility copy; `useEditorBridge` hook with `highlightField` CodeMirror extension, `CodeEditor` forwardRef, i18n across 6 locales). **12.9** SDK regeneration & full test suite (TypeScript SDK regenerated via `@hey-api/openapi-ts` covering all 17 design-sync endpoints, 36 route tests for every endpoint + auth/role enforcement, 178 total design_sync tests, `make check` all green). **13.1–13.6** mock ESP server (`services/mock-esp/` — standalone FastAPI sidecar with SQLite, 4 ESP API surfaces: Braze Content Blocks, SFMC Content Builder, Adobe Campaign Delivery, Taxi Templates; per-ESP auth patterns; 44 seeded templates with ESP-specific personalization syntax). **13.7** ESP sync protocol, model & migration (`ESPSyncProvider` Protocol, `ESPConnection` model with Fernet-encrypted credentials, BOLA-safe repository, `ESPSyncConfig` registered on Settings, Alembic migration). **13.8** per-ESP sync providers (4 `httpx.AsyncClient` implementations: `BrazeSyncProvider`, `SFMCSyncProvider`, `AdobeSyncProvider`, `TaxiSyncProvider`). **13.9** sync service & routes (`ConnectorSyncService` — credential validation, encrypted storage, BOLA, template import/push; 8 REST endpoints at `/api/v1/connectors/sync/` with auth + rate limiting). **13.10** frontend ESP sync UI (2-tab connectors page Export History | ESP Sync, 7 new components: `ESPConnectionCard`, `CreateESPConnectionDialog` with dynamic credential fields per provider, `ESPTemplateBrowser` with search, `ESPTemplatePreviewDialog` with sandboxed iframe, `PushToESPDialog`; 8 SWR hooks in `use-esp-sync.ts`; "Push to ESP" button in workspace toolbar; 65 i18n keys across 6 locales; `make check-fe` green). **13.11** tests, SDK & Docker integration (93 new tests: protocol conformance for 4 providers, service unit tests with BOLA/encryption/CRUD, route tests for 8 endpoints with auth + error scenarios; SDK regenerated with sync types; 111 total connectors tests). **14.1** checkpoint storage layer (`CheckpointStore` protocol, `PostgresCheckpointStore`, `CheckpointData` frozen dataclass, `serialize_run`/`restore_run` round-trip, `BlueprintCheckpoint` model + Alembic migration, `checkpoints_enabled` config flag, 9 tests). **14.2** engine checkpoint integration (`checkpoint_store` param on `BlueprintEngine`, `_save_checkpoint()` fire-and-forget after each node, `BlueprintService` wiring with opt-in config, 4 engine integration tests, 13 total checkpoint tests). **14.5** checkpoint cleanup & observability (`checkpoint_cleanup.py` — `cleanup_old_checkpoints()` + `cleanup_completed_runs()` + `CheckpointCleanupPoller` 24h DataPoller, structured logging with `size_bytes`/`duration_ms`/`age_seconds`, `GET /runs/{run_id}/checkpoints` endpoint with admin/developer auth + rate limiting, `checkpoint_count`/`resumed_from` on `BlueprintRunResponse`, poller in `main.py`, 13 new tests). **14.3** resume from checkpoint (`next_node_name` on `CheckpointData`, `BlueprintEngine.resume()` with validation — blueprint mismatch, missing node, terminal checkpoint, `_execute_from()` shared loop extraction, `BlueprintService.resume()` with `_build_engine`/`_build_response` helpers, `BlueprintResumeRequest` schema, `POST /api/v1/blueprints/resume` endpoint with auth + rate limiting, `resumed_from` field on `BlueprintRun`, 12 tests). **14.4** multi-pass pipeline checkpoints (`PipelineCheckpoint` dataclass, `PipelineCheckpointCallback` protocol, `_Adapter` bridging `CheckpointStore` to pipeline callbacks, compound node names `scaffolder:<pass_name>` with +100 index offset, `ScaffolderPipeline` skip-completed-passes on retry via `context.iteration > 0`, 6 pipeline checkpoint tests). **14.6** frontend run history & resume UI (Resume button on failed runs in `runs-list.tsx`, `run-checkpoints.tsx` expandable timeline, resume in `use-blueprint-run.ts` hook, `checkpoint_count`/`resumed_from` on types, i18n across 6 locales). **14.7** tests & documentation (47 checkpoint tests across 4 files: `test_checkpoint.py` 11, `test_checkpoint_cleanup.py` 13, `test_resume.py` 15 with 4 route tests, `test_pipeline_checkpoint.py` 8; ADR-006 Checkpoint & Recovery in `docs/ARCHITECTURE.md`; SDK regenerated). **15.1** typed handoff schemas (`AgentHandoff` base + per-agent typed payloads in `app/ai/blueprints/handoff.py` — `ScaffolderHandoff`, `DarkModeHandoff`, etc., `uncertainties` field, `format_upstream_constraints()` prompt injection, backward-compatible raw string wrapping, 8 agent nodes updated, 15 tests). **15.2** phase-aware memory decay (`phase` column on `Project` — active/maintenance/archived, per-phase decay config: active=60d, maintenance=14d, archived=3d, `apply_phase_aware_decay()` SQL with project JOIN + global fallback, `judge_functional_equivalence()` lightweight LLM intent judge — failure-safe, `_run_intent_merging()` embedding + LLM two-stage compaction, `intent_similarity_threshold=0.85`, `intent_merged_count` on `CompactionStats`, Alembic migration, 9 new tests). **15.3** adaptive model tier routing (`RoutingHistoryEntry` model + `RoutingHistoryRepository` in `app/ai/routing_history.py`, `resolve_adaptive_tier()` — MIN_SAMPLES=10, DOWNGRADE>0.9, UPGRADE<0.7, `resolve_model_adaptive()` in `routing.py`, `routing_history_repo` on `BlueprintEngine` with fire-and-forget recording, LAYER 12 effective tier injection, `BaseAgentService.process()` checks `effective_tier`, `AI__ADAPTIVE_ROUTING_ENABLED` config flag default off, Alembic migration, 16 tests). **15.4** auto-surfacing prompt amendments (`AmendmentSuggester` in `app/ai/agents/evals/amendment_suggester.py` — groups eval failures by agent + failure category, generates SKILL.md amendment diffs via LLM for clusters with 3+ occurrences, outputs to `traces/suggestions/`, `make eval-suggest` command, integrated into `make eval-full` pipeline, review-only — no auto-application). **15.5** bidirectional knowledge graph pre-query (`KnowledgePrefetch` service in `app/ai/agents/knowledge_prefetch.py` — `PrefetchResult` dataclass, `prefetch_prior_outcomes()` with Redis caching + tenant isolation via project_id, `format_prefetch_context()` advisory markdown, LAYER 13 in `BlueprintEngine._build_node_context()` — agentic nodes only, `COGNEE__PREFETCH_ENABLED` config flag default off, 18 tests). **16.1** query router intent classification (`QueryRouter` in `app/knowledge/router.py` — `QueryIntent` enum with 5 intents: compatibility, how_to, template, debug, general; `ClassifiedQuery` frozen dataclass; two-tier classification: regex-first with pre-compiled ontology patterns + optional LLM fallback for low-confidence queries; entity extraction resolves client names/CSS properties to ontology IDs via `_property_id_from_css()`; cross-intent suppression scoring; `KnowledgeService.search_routed()` routes to `_search_compatibility()` with ontology context prepend, `_search_debug()` with client_quirks domain, `_search_components()` with best_practices domain; `POST /api/v1/knowledge/search/routed` with auth + rate limiting; `SearchResponse.intent` optional field; Knowledge agent uses routed search; config: `KNOWLEDGE__ROUTER_ENABLED`, `KNOWLEDGE__ROUTER_LLM_FALLBACK`, `KNOWLEDGE__ROUTER_LLM_MODEL`; ships disabled by default; 51 tests). **16.2** structured compatibility queries via ontology (`OntologyQueryEngine` in `app/knowledge/ontology/structured_query.py` — `CompatibilityAnswer` + `ClientSupportResult` frozen dataclasses, `query_property_support()` returns structured support matrix per client, `query_client_limitations()` lists unsupported CSS, `find_safe_alternatives()` with client filtering, `format_as_search_results()` backward-compatible `SearchResult` output; `find_property_by_name()` + `find_client_by_name()` fuzzy lookups on `OntologyRegistry`; `_search_compatibility()` rewritten — structured ontology answer bypasses vector search entirely, falls back when entities don't resolve; 19 new tests, 70 total router+structured tests). **16.3** code-aware HTML chunking (`chunk_html()` in `app/knowledge/chunking_html.py` — `HTMLChunkResult` dataclass, `is_html_content()` detection, 3-phase extraction: `<style>` blocks → MSO conditionals → structural body splitting by `<table>`/`<div>`/`<section>`, recursive nested splitting with `_merge_small_chunks()`, graceful `chunk_text()` fallback on parse errors; `section_type` + `summary` nullable columns on `DocumentChunk` + Alembic migration; `KnowledgeService.ingest_document()` routes HTML to `chunk_html()` via `html_chunking_enabled` config flag; 26 tests). **16.5** CRAG validation loop (`CRAGMixin` in `app/ai/agents/validation_loop.py` — `_crag_validate_and_correct()` detects unsupported CSS via `unsupported_css_in_html()`, retrieves ontology fallbacks, re-generates with LLM correction; severity filtering (`error`/`warning`/`info`), failure-safe (LLM errors return original HTML), HTML size cap 100K, output sanitized via `sanitize_html_xss()`; integrated into `BaseAgentService.process()` via `isinstance(self, CRAGMixin)` check, `ScaffolderService` + `OutlookFixerService` inherit mixin; config: `KNOWLEDGE__CRAG_ENABLED` default off, `KNOWLEDGE__CRAG_MIN_SEVERITY`; 8 tests). **16.4** template/component retrieval (`ComponentSearchService` in `app/knowledge/component_search.py` — searches `Component` table with latest `ComponentVersion` join, compatibility filtering via JSON column, `search_by_embedding()` pgvector cosine distance; `ComponentRepository.search_with_compatibility()` with `escape_like()` text search + category + client compatibility filter; `_search_components()` rewritten to merge component results with top-3 knowledge base results; `search_embedding` Vector(1024) column on `Component` + Alembic migration; backward-compatible `SearchResult` output with synthetic `document_id=0`, `domain="components"`; 19 tests). **16.6** multi-representation indexing (`ChunkSummarizer` in `app/knowledge/summarizer.py` — deterministic CSS summary (selectors + properties), deterministic MSO description (condition + element counts), LLM summary for HTML body sections via shared `httpx.AsyncClient` with concurrency semaphore; `ingest_document()` embeds summaries instead of raw content when `multi_rep_enabled=True`, stores original content for search retrieval; config: `KNOWLEDGE__MULTI_REP_ENABLED` default off, `KNOWLEDGE__MULTI_REP_MODEL`, `KNOWLEDGE__MULTI_REP_API_KEY`; 24 tests). **17.1** Playwright CLI screenshot service (`app/rendering/local/` — `LocalRenderingProvider` via `npx playwright screenshot` subprocess, 5 client profiles: Gmail style-stripping, Outlook 2019 Word-engine CSS injection, Apple Mail WebKit, Outlook Dark, Mobile iOS; `_prepare_html()` CSS injection + style stripping; `capture_screenshot()` async subprocess with timeout; `POST /api/v1/rendering/screenshots` with `require_role("developer")` + `5/minute` rate limit; `ScreenshotRequest`/`ScreenshotResponse` with base64 PNGs; config: `RENDERING__SCREENSHOTS_ENABLED` default off, `RENDERING__SCREENSHOT_MAX_CLIENTS`, `RENDERING__SCREENSHOT_TIMEOUT_MS`; 25 tests). **17.2** ODiff visual regression baseline system (`app/rendering/visual_diff.py` — `DiffResult` dataclass + `run_odiff()` ODiff subprocess wrapper + `compare_images()` high-level entry point; `ScreenshotBaseline` model with `LargeBinary` storage + unique constraint on `(entity_type, entity_id, client_name)`; `ScreenshotBaselineRepository` with `get_by_entity`, `list_by_entity`, `upsert`, bulk `delete_by_entity`; `VisualDiffService` methods on `RenderingService` — `visual_diff()` with base64 validation, `list_baselines()`, `update_baseline()` with SHA-256 hashing; 3 endpoints: `POST /visual-diff` with `require_role("developer")` + `10/minute`, `GET /baselines/{entity_type}/{entity_id}` + `30/minute`, `PUT /baselines/{entity_type}/{entity_id}` + `10/minute`; `VisualDiffRequest`/`VisualDiffResponse` with `max_length=14M` on base64 fields, `VALID_ENTITY_TYPES` whitelist; config: `RENDERING__VISUAL_DIFF_ENABLED` default off, `RENDERING__VISUAL_DIFF_THRESHOLD` default 0.01; 31 tests). **17.3** VLM Visual Analysis Agent — 10th AI agent (`app/ai/agents/visual_qa/` — `VisualQAService(BaseAgentService)` with multimodal VLM calls, `VisualQARequest`/`VisualQAResponse`/`VisualDefect` Pydantic schemas, `DetectedDefect`/`VisualQADecisions` frozen dataclasses, `parse_decisions()` JSON extraction with float safety, `enrich_with_ontology()` CSS fallback cross-referencing; `VisualQANode` blueprint node runs after export as optional advisory validation — does NOT modify HTML, defects go into `AgentHandoff.warnings`; `VisualQAHandoff` registered in `HANDOFF_PAYLOAD_TYPES` with `format_upstream_constraints()` formatting; `VisualQAJudge` with 5 criteria (defect_detection_accuracy, fix_correctness, false_positive_rate, client_coverage, severity_calibration); 10 synthetic test cases; config: `AI__VISUAL_QA_ENABLED` default off, `AI__VISUAL_QA_MODEL`, `AI__VISUAL_QA_CLIENTS`; 14MB screenshot size cap; 45 tests). **17.5** Frontend Visual QA Dashboard (`cms/apps/web/src/components/visual-qa/` — 6 components: `VisualQADialog` 56rem dialog with 3 tabs Screenshots/Diff/Baselines, `ClientComparisonGrid` responsive 3-col screenshot grid, `DiffOverlay` canvas-based diff toggle with region rectangles, `DefectAnnotation` stub for VLM defect overlays, `BaselineManager` with per-client upsert + bulk update + role gating, `VisualQAPanelTab` compact sidebar summary; `use-visual-qa.ts` — 4 SWR hooks: `useCaptureScreenshots`, `useVisualDiff`, `useBaselines`, `useUpdateBaseline`; integrated into workspace QA panel + component detail dialog; 45 i18n keys across 6 locales; `make check-fe` green). **17.4** Auto-Fix Pipeline Integration (`app/ai/agents/visual_qa/correction.py` — `correct_visual_defects()` LLM-based HTML correction with ontology fallbacks + VLM suggested fixes; `VisualQANode` auto-fix orchestration: detect → correct → re-render → verify score improvement → accept/reject; `VisualQAHandoff` extended with `corrections_applied` + `pre_fix_score`; config: `AI__VISUAL_QA_AUTOFIX_ENABLED` default off, `AI__VISUAL_QA_AUTOFIX_MAX_ROUNDS`; failure-safe — original HTML preserved on any error; 12 tests). **17.6** Tests & SDK Integration (`app/rendering/tests/test_routes.py` — 28 route tests for all 8 rendering endpoints with auth/role/rate-limit coverage; `app/ai/agents/evals/tests/test_visual_qa_judge.py` — 12 judge tests for prompt building + response parsing; SDK regenerated with `VisualDiff*`, `Screenshot*`, `Baseline*` types + 4 new SDK methods; `VisualQAJudge` added to `judge_runner.py` union types; pre-existing type bugs fixed in `visual_qa/service.py` + `knowledge/summarizer.py`; 40 new tests, 2639 total, `make check` all green). **18.1** Email Chaos Engine (`app/qa_engine/chaos/` — `ChaosProfile` frozen dataclass + 8 pre-built profiles: Gmail style strip, image blocked, dark mode inversion, Outlook Word engine, Gmail clipping 102KB, mobile narrow, class strip, media query strip; `ChaosEngine` orchestrates profiles + QA checks → per-profile scores + resilience score; `compose_profiles()` stacks degradations; `QAEngineService.run_chaos_test()` with `ForbiddenError` when disabled; `POST /api/v1/qa/chaos-test` with auth + `3/minute` rate limit; `ChaosTestRequest`/`ChaosTestResponse`/`ChaosProfileResult`/`ChaosFailure` schemas; `resilience_score` on `QARunResponse`; config: `QA_CHAOS__ENABLED` default off, `QA_CHAOS__DEFAULT_PROFILES`; pure BeautifulSoup transforms — no LLM, no persistence; 31 tests). **18.2** Property-Based Email Testing Framework (`app/qa_engine/property_testing/` — `EmailInvariant` Protocol + 10 invariants: SizeLimit, ImageWidth, LinkIntegrity, AltTextPresence, TableNestingDepth, EncodingValid, MSOBalance, DarkModeReady, ContrastRatio, ViewportFit; `EmailConfig` frozen dataclass + deterministic `random_email_config()` with seeded `random.Random` for CI reproducibility + Hypothesis strategies for interactive exploration; `build_email()` synthetic HTML builder with MSO conditionals, dark mode, nested tables, size padding; `PropertyTestRunner` async orchestrator with configurable invariant subset + seed + num_cases; `PropertyFailure`/`PropertyTestReport` frozen dataclasses; `QAEngineService.run_property_test()` with `ForbiddenError` when disabled; `POST /api/v1/qa/property-test` with auth + `1/minute` rate limit; `PropertyTestRequest`/`PropertyTestResponse`/`PropertyFailureSchema` Pydantic schemas with `num_cases` capped at 1000; config: `QA_PROPERTY_TESTING__ENABLED` default off, `QA_PROPERTY_TESTING__DEFAULT_CASES`, `QA_PROPERTY_TESTING__SEED`; `make test-properties` target; `hypothesis>=6.100` dependency; pure CPU — no LLM, no DB, no persistence; 46 tests). **18.3** Resilience Score Integration & Knowledge Feedback (`app/qa_engine/checks/rendering_resilience.py` — `RenderingResilienceCheck` optional QA check #12, runs chaos engine + pass/fail on `resilience_score >= threshold`, NOT in `ALL_CHECKS` to prevent recursion — `QAEngineService.run_checks()` runs it separately after 11 core checks; `app/qa_engine/chaos/knowledge_writer.py` — `ChaosKnowledgeWriter` auto-generates knowledge documents from chaos failures with title+domain deduplication via direct SQL, `_FIX_HINTS` per profile, markdown-sanitized descriptions; `KnowledgeService.ingest_text()` programmatic document ingestion without files on disk — creates Document + chunks + embeds from string; `project_id` optional field on `ChaosTestRequest` for auto-documentation; config: `QA_CHAOS__RESILIENCE_CHECK_ENABLED` default off, `QA_CHAOS__RESILIENCE_THRESHOLD` 0.7, `QA_CHAOS__AUTO_DOCUMENT` default off; non-blocking — knowledge write failures never break chaos tests; 15 new tests, 554 total qa_engine, 2731 total backend). **18.4** Frontend Chaos & Property Testing UI (`cms/apps/web/src/components/qa/` — `ChaosTestPanel` with resilience score bar, per-profile collapsible results, severity badges, "Fix" action via `useQARun`; `PropertyTestPanel` with pass/fail gauge, seed display, invariant chips, expandable failure rows with config viewer; `use-chaos-test.ts` + `use-property-test.ts` SWR mutation hooks with 120s timeout; `cms/apps/web/src/types/chaos.ts` local type interfaces; integrated into `QAResultsPanel` after Visual QA section — chaos gated on `html` prop, property testing always visible; 33 i18n keys across 8 locales; `make check-fe` green). **19.3** Lightning CSS Email Compiler (`app/email_engine/css_compiler/` — `EmailCSSCompiler` 7-stage pipeline: parse → analyze → transform → eliminate → optimize via Lightning CSS → inline → sanitize; ontology-driven `CSSConversion` rules via `get_conversions_for_property()` + `should_remove_property()`; `resolve_css_variables()` for var() resolution; `extract_styles()` preserves MSO conditionals; `inline_styles()` with existing-style precedence; `CompilationResult` frozen dataclass; `POST /api/v1/email/compile-css` with `require_role("developer")` + `30/minute` rate limit; `CSSCompileRequest`/`CSSCompileResponse`/`CSSConversionSchema` schemas; `EmailEngineConfig` with `css_compiler_enabled` + `css_compiler_target_clients`; `lightningcss>=0.3.0` dependency; pure CPU — no LLM, no DB; 31 tests, 2838 total backend). **18.5** Tests & Documentation (37 new tests across 8 files: `test_service_chaos.py` 11 service-layer tests for chaos/property flows, `test_chaos_engine.py` 5 edge cases, `test_chaos_profiles.py` 4 profile edge cases, `test_knowledge_writer.py` 4 writer edge cases, `test_resilience_check.py` 3 severity/config tests, `test_generators.py` 4 seed/config tests, `test_invariants.py` 4 invariant edge cases, `test_runner.py` 2 route error tests; ADR-007 Rendering Resilience Testing in `docs/ARCHITECTURE.md`; 2768 total backend tests). **19.1** Outlook Word-Engine Dependency Analyzer (`app/qa_engine/outlook_analyzer/` — `OutlookDependencyDetector` with 7 detection rules: VML shapes, ghost tables, MSO conditionals, MSO CSS properties, DPI images, .ExternalClass, word-wrap hacks; `OutlookModernizer` with 3 target modes: `new_outlook` (aggressive removal), `dual_support` (keep conditionals), `audit_only`; `OutlookAnalysis`/`ModernizeResult` frozen dataclasses; `POST /api/v1/qa/outlook-analysis` `10/minute` + `POST /api/v1/qa/outlook-modernize` `5/minute`; `QAOutlookAnalyzerConfig` with `enabled` + `default_target`; output sanitized via `sanitize_html_xss()`; pure regex/DOM — no LLM, no DB; 39 tests, 2838 total backend). **Next:** Phase 19.2 (Audience-Aware Outlook Migration Planner).

## Compact instructions

Preserve: current task + plan path, modified files, test results, key decisions.
