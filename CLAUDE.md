# CLAUDE.md

## Project Overview

Centralised email platform with AI agents. FastAPI backend, Next.js 16 frontend, PostgreSQL + Redis. Python 3.12+, strict MyPy + Pyright. **Vertical slice architecture** — features under `app/{feature}/`.

## Essential Commands

```bash
make dev             # Backend (:8891) + frontend (:3000)
make check           # All checks (lint + types + tests + security + golden conformance + flag audit)
make check-full      # All checks + migration lint
make golden-conformance  # Golden template conformance gate (design_sync)
make flag-audit      # Feature flag lifecycle audit (warns >90d, errors >180d)
make test            # Backend unit tests
make bench           # Performance benchmarks (CSS pipeline)
make test-collab     # CRDT collaboration tests (convergence + Hypothesis property-based)
make lint            # Format + lint (ruff — 26 rule sets)
make lint-fe         # Format + lint frontend (ESLint + Prettier)
make types           # mypy + pyright (both strict)
make check-fe        # Frontend lint + format + type-check + tests
make security-check  # Ruff Bandit security rules
make lint-numeric    # Falsy-numeric anti-pattern check (design_sync)
make lint-polling    # Check for hardcoded polling intervals in hooks
make migration-lint  # Squawk PostgreSQL migration safety
make install-hooks   # Install pre-commit hooks (format, lint, security, secrets, commit msg)
make db-migrate      # Run migrations
make db-squash       # Squash migrations to single baseline (destructive, confirmation required)
make eval-full       # Full eval pipeline (requires LLM)
make eval-check      # Eval gate (analysis + regression)
make eval-golden     # CI golden test (deterministic, no LLM)
make eval-qa-coverage # Deterministic micro-judges coverage
make eval-corrections # Generate judge correction YAML from calibration disagreements
make eval-calibration-gate # Calibration regression gate (TPR/TNR delta check)
make eval-knowledge  # Generate calibration insights for Knowledge agent RAG
make eval-adversarial # Adversarial eval cases (hostile inputs across 7 attack types)
make sync-ontology   # Sync ontology YAML → sidecar JSON
make e2e-report      # Open last Playwright HTML report
make e2e-smoke         # Smoke E2E tests (@smoke tagged, Chromium only)
make e2e-all-browsers # E2E tests on all browsers (Chromium + Firefox + WebKit)
make rendering-baselines  # Regenerate visual regression baselines
make rendering-regression # Run visual regression tests vs baselines
make snapshot-test        # Snapshot regression tests (included in make test; standalone convenience)
make snapshot-capture CASE=5  # Capture converter output for visual review
make snapshot-visual      # Visual fidelity metrics (requires Playwright, separate from CI)
```

## HTML Email Structure Rules

- **Layout:** `<table>/<tr>/<td>` for ALL structural layout. NEVER use `<div>`/`<p>` for layout (width, flex, float, columns).
- **Text content:** All text directly in `<td>` with inline styles. **NO `<p>` or `<h1>`-`<h6>` tags.** Each `<td>` must include `font-family`, `font-size`, `color`, `line-height`, and `mso-line-height-rule:exactly`.
- **Heading-like text:** Use `<td>` with larger `font-size` and `font-weight:bold` — no semantic heading tags.
- **Simple wrappers:** `<div style="text-align:center;">` inside `<td>` is fine. No layout CSS (width/flex/float) on div.
- **MSO conditionals:** Ghost table pattern for Outlook. `<div>` inside `<!--[if mso]>` blocks is expected.
- **Spacing:** `padding` on `<td>` only (universal safe combo). No margin on text elements.
- **Sanitizer:** `sanitize_web_tags_for_email()` in `app/design_sync/converter.py` — strips ALL `<p>` and `<h>` tags (merging styles into parent `<td>`), strips layout divs, preserves MSO blocks.
- See `.agents/plans/upgrade-design-sync-html-generation.md` for the full plan.

## Known Environment Issues

- External processes (linters, background agents, git hooks) may silently revert file edits. After writing/editing files, verify changes persisted before moving on. If changes disappear, re-apply and identify the reverting process.

## Linter Safety

- Ruff TCH auto-fix breaks runtime imports (SQLAlchemy, Pydantic, datetime). Never run `ruff --fix` with TCH rules enabled. Use `--no-fix` for TCH or exclude them entirely.

## Parallel Work Awareness

- Before committing, check `git diff` carefully for changes from other branches or uncommitted parallel work leaking into the current diff. Isolate only the changes relevant to the current phase/task.

## Development Guidelines

**Key imports:** `get_logger` from `app.core.logging`, `get_db` from `app.core.database`, `TimestampMixin` from `app.shared.models`, `escape_like` from `app.shared.utils`. Roles: admin, developer, viewer.

**Config:** Nested Pydantic settings, `env_nested_delimiter="__"` (e.g. `DATABASE__URL`, `AI__PROVIDER`).

**Plans:** Implementation plans in `.agents/plans/` must not exceed 700 lines. Use compact descriptions, tables, and `file:line` references — not full code blocks.

## Architecture

Backend: `app/` (VSA features). Frontend: `cms/`. Sidecars: `services/maizzle-builder/`, `services/mock-esp/`. Migrations: `alembic/`.

**9 AI Agents:** Scaffolder, Dark Mode, Content, Outlook Fixer, Accessibility, Personalisation, Code Reviewer, Knowledge, Innovation. All have 5-criteria judges + SKILL.md files. Structured output mode returns decision schemas merged via `plan_merger.py`.

**Per-Agent Sanitization:** `sanitize_html_xss(html, profile=)` applies agent-specific nh3 allowlists. 10 profiles in `app/ai/shared.py`. **Prompt Injection Guard:** `scan_for_injection()` in `app/ai/security/prompt_guard.py` scans user-supplied inputs (brief, HTML, knowledge docs) before they reach agents. 5 pattern categories, 3 modes (`SECURITY__PROMPT_GUARD_ENABLED/MODE`).

For full architecture details see `.claude/docs/architecture-deep-dive.md`. For architecture quick-reference see `.claude/rules/architecture.md`.

## Roadmap

Phases 0–36 complete. Phase 37 (golden reference library) complete — 37.1 done (14 golden reference templates). 37.2 done (YAML-indexed loader, 18 tests). 37.3 done (golden references wired into all 7 judge prompts, 22 tests). 37.4 done (re-run against file-based component output from 40.7, verdict comparison, 21/45 criteria flagged >20% flip rate). 37.5 done (540 rows human-labeled, calibration validated TPR ≥ 0.85 / TNR ≥ 0.80). Phase 38 (pipeline fidelity fix) complete — all 8 subtasks done. Phase 39 (pipeline hardening) complete — all 7 subtasks done. 39.1 Figma API enrichment, 39.2 testing infrastructure, 39.3 dual MJML path eliminated, 39.4 automated quality contracts, 39.5 custom lint rules, 39.6 component matcher improvements (`_score_candidates()` multi-candidate scoring, 3 new component types product-grid/category-nav/image-gallery, slot fill validation, `match_confidences` on `ConversionResult`), 39.7 golden template conformance gate. Phase 40 (converter snapshot regression testing) complete — all 7 subtasks done. 40.1 snapshot infrastructure, 40.2 3 active cases (MAAP 9/13, Starbucks 5/9, Mammut 12/18; approach B), 40.3 Figma design screenshot capture (`_capture_design_image()`, 13 tests), 40.4 Playwright visual regression (`test_snapshot_visual.py` — converter + reference fidelity vs `design.png`, ODiff pixel diff, HTTP server for images, Pillow resize for dimension mismatch, `visual_report.json` per case, `visual_threshold` in manifest), 40.5 CI gate, 40.6 image frame export (16 tests), 40.7 unified component resolution (89 components). Phase 41 (converter background color continuity + VLM classification) complete — all 7 subtasks done. Track A (bgcolor): 41.1 `image_sampler.py` edge color sampling (16 tests), 41.2 `bgcolor_propagator.py` adjacent-section propagation (18 tests), 41.3 text/link color inversion for dark backgrounds (12 tests), 41.4 snapshot regression cases (6 tests). Track B (VLM): 41.5 `vlm_classifier.py` per-section component matcher fallback (8 tests, `DESIGN_SYNC__VLM_FALLBACK_ENABLED`), 41.6 batch frame screenshot export (5 tests), 41.7 `VLMSectionClassifier` batch section type classification + hybrid rule/VLM merge in `analyze_layout()` — `_classify_section()` returns `tuple[EmailSectionType, float]` confidence scores, `VLMSectionClassification` dataclass, 3-rule merge (rule > 0.9 wins, UNKNOWN overridden by VLM, VLM > rule when above threshold), config flags `DESIGN_SYNC__VLM_CLASSIFICATION_ENABLED/MODEL/CONFIDENCE_THRESHOLD/TIMEOUT`, 12 tests. Phase 42 (HTTP caching, smart polling & data fetching hardening) complete — all 7 subtasks done. 42.1 done (ETag middleware `app/core/etag.py`, MD5 hash of JSON body, `304 Not Modified` on `If-None-Match` match, `Cache-Control: no-cache, must-revalidate`, 8 tests). 42.2 done (SWR fetcher 304 handling in `swr-fetcher.ts` — returns `undefined` on 304 so SWR keeps cached data, SSR empty-body edge case, 5 tests). 42.3 done (`useSmartPolling` hook in `use-smart-polling.ts` — visibility-aware `refreshInterval` for SWR, 3 states visible/blurred/hidden, SSR-safe, 6 tests). 42.4 done (`swr-constants.ts` — `POLL` 6-tier intervals, `DEDUP` 3-tier deduplication, `SWR_PRESETS` 4 option presets, all `as const` literal types, 7 tests). 42.5 done (8 polling hooks migrated in 7 files — `useSmartPolling` + `POLL.*` constants + `SWR_PRESETS.polling` spread, conditional hooks for renderings/design-sync, `SWR_PRESETS.reference` for email-clients dedup, 12 migration tests). 42.6 done (`app/core/progress.py` `ProgressTracker` thread-safe in-memory store + `ProgressEntry` dataclass + `OperationStatus` enum, `app/core/progress_routes.py` `GET /api/v1/progress/{id}` + `GET /api/v1/progress/active/list` with auth, `ProgressConfig` in config, lifespan cleanup loop, wired into rendering/qa_engine/design_sync/connectors services, `use-progress.ts` SWR hook with `useSmartPolling` + auto-stop on completion, 13 backend tests + 6 frontend tests). 42.7 done (ESLint `no-restricted-syntax` rule for hardcoded `refreshInterval` literals, `make lint-polling` grep-based fallback, `test_etag_ci.py` 3 tests verifying ETag middleware on real app `/health` endpoint, `use-workflows.ts` + `use-voice-briefs.ts` migrated to `useSmartPolling` + `POLL.*`, wired into `check-fe`). Phase 43 (judge feedback loop) complete — all 6 subtasks done. 43.1 done (`judge_corrections.py` — reads human labels + verdicts JSONL, extracts FP/FN disagreements, generates per-agent correction YAML to `traces/corrections/`, caps 3 per criterion sorted by reasoning length, `make eval-corrections`, 10 tests). 43.2 done (`format_corrections_section()` in `base.py` — loads correction YAML, FP-prioritized, 1500-token budget, `@lru_cache` with mtime staleness, `build_system_prompt()` wrapper replacing direct `SYSTEM_PROMPT_TEMPLATE.format()` in all 11 judge files, `--no-corrections` flag in `judge_runner.py` for A/B runs, 9 tests). 43.3 done (`judges/skills/` directory — 5 domain knowledge skill files (MSO conditionals, email layout detection, dark mode completeness, ESP syntax validation, copy quality boundaries), 11 per-judge YAML manifests mapping criteria→skill files, `load_judge_skills()`/`format_skills_section()` in `base.py` with `lru_cache` + mtime staleness, 1000-token per-file + 2000-token total budget, `{skills_block}` in `SYSTEM_PROMPT_TEMPLATE`, `--no-skills` A/B flag, 6 tests). 43.4 done (`scripts/generate-calibration-knowledge.py` — reads all `traces/*_calibration.json`, extracts cross-agent failure patterns + early-warning criteria + disagreement examples, generates structured Markdown to `data/knowledge/judge_calibration_insights.md`, `make eval-knowledge`, 11 tests). 43.5 done (`calibration_tracker.py` — loads `*_calibration.json`, extracts per-criterion TPR/TNR metrics, compares against `traces/calibration_baseline.json`, flags regressions >5pp, `CalibrationDelta` dataclass in `schemas.py`, `make eval-calibration-gate` wired into `eval-check`, `--update-baseline` flag, 8 tests). 43.6 done (`correction_impact.py` — A/B comparison of judge calibration with/without corrections, `load_calibration_pair()` + `classify_impact()` + `build_impact_report()`, per-criterion TPR/TNR deltas with improved/degraded/no_change verdicts, `make eval-judge-no-corrections` + `make eval-calibrate-no-corrections` + `make eval-correction-impact` targets, 16 tests). Phase 44 (workflow hardening & operational maturity) complete — all 12 subtasks done. 44.1 E2E smoke CI, 44.2 Renovate, 44.3 feature flag lifecycle, 44.4 adversarial eval pass (`adversarial.py` 7 attack types × 9 agents, YAML fixtures, `--include-adversarial` runner flag, `--adversarial` judge flag, `adversarial_regression.py` auto-regression from failures, `error_analysis.py` adversarial pass rate WARN/FAIL thresholds, `make eval-adversarial`, 16 tests), 44.5 operational runbooks, 44.6 migration squash, 44.7 CRDT tests, 44.8 SDK drift detection, 44.9 observability stack (Grafana+Loki+Promtail), 44.10 contributing guide, 44.11 prompt injection detection (`app/ai/security/prompt_guard.py` — 5 pattern categories, 3 modes warn/strip/block, `InjectionScanResult`, wired into `BlueprintEngine._build_node_context()` + `HtmlImportAdapter.parse()` + `KnowledgeService.ingest_document()`, `SecurityConfig` with `SECURITY__PROMPT_GUARD_ENABLED/MODE`, `PromptInjectionError`, 15 tests), 44.12 PII redaction (`app/core/redaction.py` — 5 compiled regex patterns matching `sanitize.py`, `redact_event_dict` structlog processor before JSONRenderer, `redact_value()` recursive for eval trace dicts, wired into `production_sampler.py` enqueue + verdicts + `runner.py` 4 trace write points, `LOGGING__PII_REDACTION` config default true, 14 tests) all complete. Phase 45 (scheduling, notifications & build debounce) complete — all 6 subtasks done. 45.1 done (`app/scheduling/` — `CronScheduler` asyncio background task with Redis persistence + leader election, `@scheduled_job(cron=...)` decorator + registry, `SchedulingConfig` with `SCHEDULING__ENABLED`, 5 admin API endpoints at `/api/v1/scheduling`, `croniter>=2.0` dependency, 12 tests). 45.2 done (`app/scheduling/jobs/qa_sweep.py` — `@scheduled_job(cron="0 6 * * *")` daily sweep of all active templates, runs `html_validation`/`css_support`/`css_audit` checks on latest version, regression detection against previous sweep baseline with configurable threshold, Redis storage with 30d TTL, bounded concurrency via semaphore, `make qa-sweep` manual target, 8 tests). 45.3 done (`app/scheduling/jobs/ontology_sync.py` — `@scheduled_job(cron="0 3 * * 0")` weekly Sunday 03:00 UTC, wraps `CanIEmailSyncService.sync()`, skips when `ontology_sync.enabled=False`, Redis storage with 30d TTL, 3 tests; `app/scheduling/jobs/rendering_baselines.py` — `@scheduled_job(cron="0 4 1,15 * *")` biweekly 1st/15th 04:00 UTC, wraps `BaselineGenerator.generate_baselines()`, skips when `rendering.provider="mock"`, 3 tests). 45.4 done (`app/notifications/` — `NotificationChannel` Protocol + `Notification`/`NotificationResult` dataclasses, `SlackChannel` Block Kit webhook, `TeamsChannel` Adaptive Card webhook, `EmailChannel` aiosmtplib, `NotificationRouter` multi-channel broadcast with fire-and-forget + logging, `NotificationsConfig` with per-channel enable/URL/timeout, `notification_config` JSON column on `Project`, 14 tests). 45.5 done (`app/notifications/emitter.py` — `emit_notification()` with Redis dedup 5-min window, fire-and-forget, wired into `BlueprintEngine` completion/failure, `QAService` gate failure, `ApprovalService` requested/decided, `RenderingService` gate failure + regression detected, `CronScheduler` job failure, 10 tests). 45.6 done (`app/core/debounce.py` — `Debouncer` class with Redis-backed token-based distributed debounce, `_worker()` background task with sleep-then-check pattern, `_debounce_tasks` set for GC prevention, immediate execution when `DEBOUNCE__ENABLED=false`, exception-safe callbacks; `DebounceConfig` with `enabled`/`default_window_ms`/`figma_webhook_window_ms`/`qa_trigger_window_ms`/`rendering_trigger_window_ms`; `app/design_sync/webhook.py` refactored — inline `enqueue_debounced_sync`/`debounced_sync_worker` replaced with `get_webhook_debouncer()` → `Debouncer`; 10 tests). Phase 46 (provider resilience & connector extensibility) complete — all 5 subtasks done — 46.1 done (`app/core/credentials.py` — `CredentialPool` round-robin rotation with `CredentialLease` context, exponential backoff cooldowns on 429/401/403, `_KeyState` Redis-backed with in-memory fallback, `NoHealthyCredentialsError`, `CredentialsConfig` with `CREDENTIALS__ENABLED/POOLS`, `get_credential_pool()` singleton registry, 14 tests). 46.2 done (`app/ai/adapters/anthropic.py` + `openai_compat.py` — `_pool` field + per-key client cache + lease lifecycle in `complete()`/`stream()`, `isinstance(pools, dict)` MagicMock guard, per-request `Authorization` header override for OpenAI-compat; `app/ai/fallback.py` — `NoHealthyCredentialsError` added to `_is_retryable()` for provider-level fallback, 9 tests). 46.3 done (`app/connectors/braze/service.py`, `sfmc/service.py`, `adobe/service.py`, `taxi/service.py` — `_pool: CredentialPool | None` in `__init__()`, `_lease_credentials()` with bare key (Braze/Taxi) or `json.loads()` + schema validation (SFMC/Adobe), pool-aware `export()` with `report_success()`/`report_failure()` lifecycle + `ExportFailedError` wrapping, `app/connectors/service.py` catches `NoHealthyCredentialsError` → `ExportFailedError`, DB credentials priority over pool, 7 tests). 46.4 done (`app/core/credentials_routes.py` — `GET /api/v1/credentials/health` admin-only endpoint with `CredentialHealthResponse`/`ServiceHealthReport`/`KeyHealthReport` Pydantic schemas, async `pool_status()` on `CredentialPool`, `get_all_pools()` registry function, only SHA-256[0:12] hashes exposed, `use-credentials-health.ts` SWR hook, `CredentialHealthCard.tsx` traffic-light card in ecosystem dashboard, 6 tests). 46.5 done (`app/connectors/plugin_bridge.py` — `register_plugin_connector()`/`register_plugin_sync_provider()`/`unregister_plugin_connector()` runtime bridge into `SUPPORTED_CONNECTORS` + `PROVIDER_REGISTRY` with built-in priority on duplicates; `app/plugins/api.py` — `PluginConnectorAPI` with `register_provider()`/`register_sync_provider()` + `hasattr` protocol validation; `app/plugins/registry.py` — `_connector_names` tracking, `_wire_connector()` with `PluginConflictError`, `_unregister_connectors()` cleanup in `unregister()`/`disable()`, `enable()` re-wires; `plugins/sample-esp-connector/` sample plugin with manifest + `SampleConnectorProvider`; 10 tests). Phase 47 (VLM visual verification loop & component library expansion) complete — all 10 subtasks done. 47.2 done (`app/design_sync/visual_verify.py` — `compare_sections()` with ODiff pre-filter + VLM structured correction extraction, `SectionCorrection`/`VerificationResult` dataclasses, bounded cache, 5 config fields `DESIGN_SYNC__VLM_VERIFY_*`, 10 tests). Track A: visual verify loop (section-level screenshot cropping, VLM section-by-section comparison with ODiff pre-filter, deterministic correction applicator, render→compare→correct iteration loop with convergence detection, `DESIGN_SYNC__VLM_VERIFY_ENABLED`). Track B: component library expansion (89→150 components done in 47.6 — countdown timers, testimonials, pricing tables, zigzag layouts, etc.) + extended matcher scoring + AI custom component generation via Scaffolder for unmatched sections (`DESIGN_SYNC__CUSTOM_COMPONENT_ENABLED`). Track C: diagnostic trace enhancement. 47.1 done (`app/rendering/screenshot_crop.py` — `crop_section()` Pillow utility, bounds clamping, graceful fallback, 4 tests). 47.3 done (`app/design_sync/correction_applicator.py` — `apply_corrections()` pure function with `CorrectionResult` dataclass, lxml+CSSSelector section-scoped style/content/image correction, CSS sanitization, confidence threshold gating, 10 tests). 47.4 done (`run_verification_loop()` in `visual_verify.py` — iterative render→compare→correct cycle with `VerificationLoopResult` dataclass, convergence detection, regression revert, 3 config fields `VLM_VERIFY_MAX_ITERATIONS/TARGET_FIDELITY/CONFIDENCE_THRESHOLD`, 8 tests). 47.5 done (`_apply_verification()` in `converter_service.py` — wired into `convert_document_mjml()` async path, `design_screenshots` param on entry points, 3 verification metadata fields on `ConversionResult`, 2 new config fields `VLM_VERIFY_CORRECTION_CONFIDENCE/CLIENT`, exception-safe with graceful skip, 6 tests). 47.6 done (component library expansion 89→150 manifest entries + 1 inline = 151 `COMPONENT_SEEDS`, 61 new HTML files in `email-templates/components/` — countdown timers ×4, testimonials ×3, pricing tables ×3, team bios ×2, event cards ×3, video placeholders ×3, FAQ ×2, social proof ×4, zigzag ×3, hero variants ×5, mosaic ×2, card grids ×3, sidebars ×2, nav variants ×3, announcement bars ×3, app download ×2, loyalty ×2, divider/spacer variants ×3, interactive ×4; all table/tr/td layout, data-slot attrs, MSO conditionals, dark mode classes; `test_new_components.py` 696 tests — 9 parametrized structural checks + 5 slot fill + 3 security checks). 47.7 done (`_score_extended_candidates()` in `component_matcher.py` — 7 new component type detectors: countdown-timer (time patterns + headings, 0.92), testimonial (quote chars + avatar ≤100px, 0.9), pricing-table (currency + 2+ col_groups + buttons, 0.93), video-placeholder (16:9 aspect ratio + button, 0.88), event-card (date patterns + images, 0.85), faq-accordion (alternating Q?/A texts, 0.88), zigzag-alternating (3+ mixed col_groups, 0.9); module-level compiled regex patterns `_TIME_PATTERN`/`_TIME_UNIT_PATTERN`/`_CURRENCY_PATTERN`/`_DATE_PATTERN`/`_QUOTE_CHARS`; extended scorer wins over base when match found (specific content signals always beat generic heuristics); 12 tests — 10 positive/negative + 2 regression). 47.8 done (`app/design_sync/custom_component_generator.py` — `generate_custom_component()` async function, builds Scaffolder brief from `EmailSection` data + `ExtractedTokens`, `scan_for_injection()` on brief text, `_try_custom_generate()` sync bridge in `converter_service.py` render loop via `ThreadPoolExecutor`, 4 config fields `DESIGN_SYNC__CUSTOM_COMPONENT_ENABLED/CONFIDENCE_THRESHOLD/MODEL/MAX_PER_EMAIL`, graceful fallback to template on failure, 14 tests). 47.9 done (31 new tests across 4 files — `test_visual_verify.py` +9 multi-section/fidelity/cache/VLM-error/parse tests, `test_correction_applicator.py` +12 layout/CSS-sanitization/selector/image/content/multi-section tests, `test_verification_loop.py` +7 exception-path/boundary/zero-section tests, `test_snapshot_regression.py` +3 parametrized `TestVerificationMetadata` tests × 3 cases verifying `_apply_verification` metadata/fidelity/corrections). 47.10 done (`SectionTrace` extended with 5 fields: `vlm_classification`, `vlm_confidence`, `verification_fidelity`, `corrections_applied`, `generation_method`; `DiagnosticReport` extended with `verification_loop_iterations`, `final_fidelity`; `build_section_traces()` accepts optional verification/generation/VLM metadata dicts; `run_from_structure()` extracts per-section fidelity from `VerificationLoopResult`; 3 structured logging events `diagnose.verification_metadata_attached`/`custom_generation_traced`/`vlm_classification_traced`; 4 tests). Fidelity ladder: Phase 40 ~85% → Phase 41 ~93% → Phase 47 verify loop ~97% → Phase 47 component expansion ~99%. Phase 48 (agent pipeline DAG, adversarial quality loops & cross-repo pattern adoption) complete — all 13 subtasks done. 48.1 done (`app/ai/pipeline/dag.py` — `PipelineNode` frozen dataclass, `PipelineDag` with Kahn's toposort at construction + `validate()` artifact wiring/agent checks; `app/ai/pipeline/registry.py` — `PipelineRegistry` singleton YAML loader with `get()`/`register()`/`list_all()`, `get_pipeline_registry()` factory; 4 YAML templates in `app/ai/pipeline/templates/` (full-build 7 nodes, quick-fix, qa-only, design-import); `CyclicDependencyError` exception; `PipelineConfig` with `PIPELINE__DEFAULT_TEMPLATE`/`CUSTOM_DIR`; 28 tests — 14 DAG + 14 registry). 48.2 done (`app/ai/pipeline/executor.py` — `PipelineExecutor` concurrent DAG execution with `asyncio.gather()` per topological level, `AgentRunner` Protocol for injected agent resolution, `NodeTrace`/`PipelineResult` frozen dataclasses, semaphore-bounded concurrency (`PIPELINE__MAX_CONCURRENT_AGENTS`), contract validation per node via `ContractValidator`, fail-fast on `contract_strict`, resume via `completed_levels`, `_merge_html_outputs()` safety net for custom templates; `PipelineExecutionError` in exception hierarchy; `PipelineConfig` extended with `enabled`/`max_concurrent_agents`/`merge_strategy`; 16 tests). 48.3 done (`app/ai/pipeline/artifacts.py` — `Artifact` frozen base + 7 concrete types `HtmlArtifact`/`BuildPlanArtifact`/`QaResultArtifact`/`CorrectionArtifact`/`DesignTokenArtifact`/`ScreenshotArtifact`/`EvalArtifact`; `ArtifactStore` typed get/put/snapshot + optional Redis persistence; `ArtifactAdapter` `@runtime_checkable` Protocol + `ADAPTER_REGISTRY` + 10 per-agent adapters in `app/ai/pipeline/adapters/`; `AgentHandoff.to_artifacts()`/`from_artifact_store()` bridge methods; `ArtifactNotFoundError`/`ArtifactTypeError` in exception hierarchy; 22 tests). 48.5 done (`app/ai/pipeline/contracts.py` — `Contract`/`Assertion`/`ContractResult`/`AssertionFailure` frozen dataclasses, `ContractValidator` with 8 built-in checks (html_valid/min_size/max_size/has_table_layout/dark_mode_present/no_critical_qa/fidelity_above/no_xss), `_CHECK_REGISTRY` dispatch, `load_contract()` YAML loader with `@lru_cache`, 3 predefined contracts in `contract_defs/` (html_valid, no_critical_issues, fidelity_above_threshold), `PipelineConfig` extended with `contract_retry`/`contract_strict`; 12 tests). 48.6 done (`app/components/tree_schema.py` — `EmailTree` Pydantic model with `TreeMetadata`/`TreeSection`/`SlotValue` discriminated union (Text/Image/Button/Html), `validate_tree_against_manifest()` cross-validation, `export_json_schema()`/`write_json_schema()` helpers, `app/components/schemas/email-tree.json` generated schema, MCP resource `hub://component-tree-schema`, 10 tests). 48.4 done (`app/ai/agents/evaluator/` — `EvaluatorAgentService(BaseAgentService)` adversarial evaluation of other agents' output, different-provider enforcement (generator=openai → evaluator=anthropic), `EvalVerdict` with accept/revise/reject + score + structured issues, per-agent criteria YAML (scaffolder/dark_mode/accessibility/generic), `scan_for_injection()` on agent output, `EvaluatorNode` blueprint node with graceful degradation, `"revise"` EdgeCondition in engine with revision counter + `BLUEPRINT__MAX_REVISIONS` cap, `EvaluatorConfig` nested in `AIConfig` (`AI__EVALUATOR__ENABLED/PROVIDER/CRITERIA_DIR/MAX_TOKENS`), 14 tests). 48.7 done (`app/ai/agents/scaffolder/tree_builder.py` — `TreeBuilder` deterministic `EmailTree` assembly from 3-pass outputs, `ComponentSelection` frozen dataclass, `_infer_slot_value()` typed slot inference (Text/Image/Button/Html) from manifest slot_type + content heuristics, `_build_manifest_index()` cached manifest reader; `pipeline.py` `execute_tree()` 3-pass tree pipeline (layout selects components from 150+ manifest, content fills per-component slots, design reuses `_design_pass()`) with parallel content+design via `asyncio.gather`; `prompt.py` `build_component_manifest_prompt()`/`build_tree_layout_system_prompt()`/`build_tree_content_system_prompt()`; `service.py` `_process_tree()` routing from `_process_structured()` on `output_mode="tree"`; `schemas.py` `output_mode` extended to `Literal["html","structured","tree"]`, `tree: dict | None` on response, `html` default `""`; config `AI__SCAFFOLDER_TREE_MODE`/`AI__SCAFFOLDER_TREE_MANIFEST_BUDGET`; 13 tests). 48.8 done (`app/components/tree_compiler.py` — `TreeCompiler` deterministic EmailTree→HTML compiler, `CompiledEmail` frozen dataclass, lxml DOM slot filling (TextSlot→`html.escape`, ImageSlot→`<img>` attrs, ButtonSlot→`<a>` with URL scheme whitelist, HtmlSlot→`sanitize_component_html()`), `_parse_style`/`_serialize_style` inline CSS manipulation, email-shell assembly with regex-based `data-slot` injection (title/preheader/body), dark palette `@media (prefers-color-scheme: dark)` injection, per-section MD5-keyed cache `(slug, slot_hash, override_hash)`, `CompilationError(DomainValidationError)` in exception hierarchy, 14 tests). 48.9 done (`app/qa_engine/meta_eval.py` — `MetaEvaluator` class with per-check confusion matrix (TP/FP/TN/FN/precision/recall/F1/specificity), `LabeledSample`/`AdversarialEmail`/`CheckEvalResult`/`MetaEvalReport` frozen dataclasses, threshold recommendation engine (FP > 10% or FN > 5%), `load_golden_samples()` reads `expected_qa` labels from golden reference YAML, `report_to_dict()`/`save_report()`/`load_latest_report()` JSON persistence to `traces/`, CLI entry point; `app/qa_engine/meta_eval_routes.py` — admin-only `POST /api/v1/qa/meta-eval` + `GET /api/v1/qa/meta-eval/latest` with rate limiting; `QAMetaEvalConfig` in config (`QA_META_EVAL__ENABLED/FP_THRESHOLD/FN_THRESHOLD`); `meta_eval_to_calibration_results()` bridge in `qa_calibration.py`; `expected_qa` labels on all 14 golden references in `index.yaml`; 10 tests). 48.10 done (`app/qa_engine/synthetic_generator.py` — `SyntheticEmailGenerator` with 11 targeted defect injectors (one per QA check, skip css_audit/deliverability/css_support), `SyntheticEmail`/`SyntheticManifest` frozen dataclasses, `load_base_templates()` loads 15 golden templates, `generate_adversarial_set()` produces 55 emails (5 per check × 11 checks) with difficulty cycling (2 easy/2 medium/1 hard), `save()` writes manifest.json + per-email HTML files, `QASyntheticConfig` with `count_per_check`/`output_dir`, `data/synthetic-adversarial/` output dir, 12 tests). 48.11 done (`app/mcp/optimization.py` — `MCPResponseCache` in-memory LRU with blake2b keys + TTL expiry + thread-safe `OrderedDict`, `CACHEABLE_TOOLS`/`WRITE_TOOLS` frozensets, `compress_schema()` strips nested descriptions/titles/defaults + collapses single `anyOf`, `SchemaRegistry` pre-compressed schemas, `batch_execute()` concurrent tool calls with semaphore(5) + cache-first, `ToolCall`/`ToolResult`/`CacheEntry` frozen dataclasses; `app/mcp/server.py` — `_wrap_cacheable_tools()` cache decorator on 14 read-only tools, `_register_schemas()` schema compression at startup, `mcp_batch_execute` meta-tool with input validation; `MCPConfig` extended with `cache_enabled`/`cache_max_size`/`cache_ttl`/`compress_schemas`; 12 tests). 48.12 done (`app/knowledge/proactive_qa.py` — `ProactiveWarning` frozen dataclass, `FailureExtractor` with blake2b dedup + severity mapping, `ProactiveWarningInjector` with semantic memory query by component+client, `_recall_component_warnings()` pgvector search with metadata filtering; `app/knowledge/proactive_qa_routes.py` — `GET /api/v1/knowledge/proactive-warnings` + `GET /api/v1/knowledge/failure-graph` with auth + rate limiting; `ProactiveWarningsArtifact` in `app/ai/pipeline/artifacts.py`; `PipelineExecutor._inject_proactive_warnings()` before level 0; `KnowledgeConfig` extended with `proactive_qa_enabled`/`proactive_max_warnings`/`failure_min_occurrences`; 12 tests). 48.13 done (`app/ai/hooks/` — `HookRegistry` with profile-based filtering (minimal/standard/strict), `HookEvent` StrEnum (8 events: pre/post_agent, pre/post_pipeline, pre/post_level, contract_failed, artifact_stored), `HookContext`/`HookResult` frozen dataclasses, `HookFn` async callable type; `app/ai/hooks/builtin/` — 5 built-in hooks: `cost_tracker` (minimal, per-agent token accumulation with TTL eviction), `structured_logger` (standard, JSON events for all 8 events), `progress_reporter` (standard, `ProgressTracker` updates), `adversarial_gate` (strict, evaluator agent post-agent gate with `HookAbortError`), `pattern_extractor` (strict, recurring failure pattern detection); `HookConfig` nested in `PipelineConfig` (`PIPELINE__HOOKS__PROFILE/CUSTOM_HOOK_DIR/DISABLED_HOOKS`); `HookAbortError(PipelineExecutionError)` in exceptions; `PipelineExecutor` extended with `hook_registry` param + `_fire()` helper at 7 injection points + `HookAbortError` propagation through `asyncio.gather`; 14 tests). Phase 49 (design-sync converter structural fidelity) complete — all 9 subtasks done. 49.1 done (`app/design_sync/sibling_detector.py` — `SiblingSignature` frozen dataclass (6-field structural fingerprint), `RepeatingGroup` frozen dataclass, `detect_repeating_groups()` consecutive-similar-section merger with weighted 6-dimension similarity scoring, `_SKIP_TYPES` for DIVIDER/SPACER passthrough, wired into `converter_service.py:_convert_with_components()` before `match_all()` with `group_map` for 49.2 renderer, 3 config fields `DESIGN_SYNC__SIBLING_DETECTION_ENABLED/MIN_GROUP/SIMILARITY_THRESHOLD`, 14 tests). 49.2 done (`component_renderer.py` — `_GroupSpacing` frozen dataclass, `_resolve_item_spacing()` helper deriving padding from group metadata or section padding, `render_repeating_group()` method on `ComponentRenderer` wrapping N `render_section()` calls in container `<table>` with MSO ghost table + bgcolor + dark mode class, single-section passthrough, empty group guard; `converter_service.py` — rendering loop refactored with `rendered_group_ids: set[int]` dedup via `id(group)`, group-level cache check/store keyed by first member's `node_id`, `enumerate(matches)` + `group_map.get(flat_idx)` dispatch; 12 tests). 49.3 done (`component_matcher.py` — 4 scoring fixes: editorial-2 width guard rejecting wide single col_groups as stacked layout, col-icon detector for icon-sized images ≤64px in `_score_extended_candidates()` (0.92), full-width-image vs image-block split at ≥80% container width in `_score_candidates()`, event-card keyword-based detection (`_EVENT_KEYWORD_PATTERN`/`_TIME_OF_DAY_PATTERN` regexes) removing `has_images` requirement; `layout_analyzer.py` — `_compute_content_roles()` helper populating `content_roles` tuple on `EmailSection` with text-only/text-with-icon/editorial/event-info hints, wired into `analyze_layout()` section construction; 16 new tests in `TestClassificationImprovements` + 4 updated existing tests, 122 total passing). 49.4 done (`component_renderer.py` — 15 module-level compiled regex patterns for expanded element-type matching: `_HEADING_SLOTS` (`heading|headline|title`), `_BODY_SLOTS` (`body|body_text|description|caption|subtext`), 6 heading/body semantic CSS class tuples, two-pass data-slot→class-fallback in `_replace_heading_font/color/size()` + `_replace_body_font/color/size()`, `_replace_bg_class_color()` for container bg classes, `_apply_token_overrides()` extended with `font-size` dispatch + bg class fallback on `_outer`; `component_matcher.py` — `_build_token_overrides()` emits `font-size` overrides from `TextBlock.font_size`; 14 new tests in `TestTokenOverrideExpansion` — 4 alternate data-slot tests + 4 class-based font/color + 3 font-size + bg class + no-match + regression; snapshot baselines regenerated for cases 5/6/10). 49.5 done (`layout_analyzer.py` — `ContentGroup` frozen dataclass (frame_node_id, frame_name, texts, images, buttons), `_extract_content_groups()` groups direct child FRAME/GROUP/COMPONENT nodes preserving parent-child structure (min 2 groups), `_assign_role_hints()` assigns heading/body/label via font size relative to group median, `TextBlock` extended with `source_frame_id`/`role_hint`, `child_content_groups` on `EmailSection`; `component_matcher.py` — `_headings_from_groups()`/`_bodies_from_groups()` helpers, `_fills_text_block`/`_fills_hero`/`_fills_article_card` prefer `child_content_groups` with flat fallback, `_build_column_fills_from_content_groups()` for column layouts, `_log_default_fills()` warns on placeholder slot values; `component_renderer.py` — `_PLACEHOLDER_IN_OUTPUT_RE` module-level regex + warning in `_fill_slots()` for surviving placeholders; 14 tests). 49.6 done (`figma/service.py` — `_find_subtree()` module-level node finder, `sync_tokens_and_structure()` accepts `target_node_id: str | None`, scopes `_parse_colors/_parse_typography/_parse_spacing` walks to target frame subtree via `scoped_file_data`, `build_document()` wires `selected_nodes[0]`, `DESIGN_SYNC__TOKEN_SCOPING_ENABLED` config flag, fallback to global on missing target, `protocol.py` updated signatures, 10 tests). 49.7 done (`ButtonElement` extended with `stroke_color`/`stroke_weight`/`icon_url`; `_walk_for_buttons()` extracts stroke props + icon children ≤64px; `_build_token_overrides()` emits 5 CTA-specific overrides (`background-color`/`color`/`border-radius`/`border-color`/`border-width` on `_cta` target); `_build_column_fill_html()` uses actual button text color/radius/stroke instead of hardcodes; `component_renderer.py` `_cta` target handler with `_replace_css_prop_all()`/`_replace_cta_text_color()`/`_update_vml_arcsize()` helpers; `button-filled.html`/`button-ghost.html` gain `class="cta-btn"`; 18 tests). 49.8 done (`app/design_sync/tree_bridge.py` — `build_email_tree()` converts `ComponentMatch[]` → `EmailTree`, `_fill_to_slot_value()` typed slot conversion (text/image/cta/attr), `_convert_token_overrides()` CSS dict, `_build_design_tokens()` colors/typography/dark_palette, `_extract_preheader()` from first PREHEADER section, repeating group → `TreeSection.children` with `container_bgcolor`, `_clamp_dimension()` 1-2000; `converter_service.py` — `output_format: Literal["html", "tree"]` parameter on `convert_document()`/`_convert_with_components()`, `tree: dict | None` on `ConversionResult`, tree bridge path after `match_all()` with `TreeCompiler` compilation + legacy fallback; `DESIGN_SYNC__TREE_BRIDGE_ENABLED` config flag (default false); 21 tests). 49.9 done (`app/design_sync/tests/manifest_schema.py` Pydantic manifest models `CaseManifest`/`SectionExpectations`/`ComponentExpectation`/`TokenExpectations`/`CTAExpectation`, `regression_runner.py` case discovery + conversion + metrics collection + CLI report, `test_converter_data_regression.py` 62 parametrized tests — 7 universal structural checks on converter output + 8 manifest-driven assertion classes, 4 cases discovered (MAAP/Starbucks/Mammut migrated + REFRAME 2025 reference_only), `reference_only` mode for cases with only `expected.html`, `DesignSyncConfig` `regression_dir`/`regression_strict`, `make converter-data-regression` + `make converter-data-regression-report` targets, 38 passed + 24 skipped). See `docs/TODO-completed.md` for detailed completion records.

## Compact instructions

Preserve: current task + plan path, modified files, test results, key decisions.
