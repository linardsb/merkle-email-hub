/**
 * Chaos & Property-based testing types — re-exported from the generated SDK.
 *
 * Backend source: `app/qa_engine/schemas.py`. Regenerate via `make sdk`.
 */
export type {
  ChaosFailure,
  ChaosProfileResult,
  ChaosTestRequest,
  ChaosTestResponse,
  PropertyFailureSchema,
  PropertyTestRequest,
  PropertyTestResponse,
} from "@email-hub/sdk";
