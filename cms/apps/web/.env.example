AUTH_SECRET=  # Generate with: openssl rand -base64 32
INTERNAL_API_URL=  # Optional: override backend URL for server-side calls

# Demo mode: populate all pages with realistic example data (no backend needed)
# NEXT_PUBLIC_DEMO_MODE=true
