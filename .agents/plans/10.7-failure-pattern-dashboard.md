# Plan: 10.7 Failure Pattern Dashboard

## Status: IMPLEMENTED

## Context
Failure pattern propagation (Phase 9.4) collects rich data about cross-agent QA failures in blueprint runs, but there was no frontend visibility. This dashboard shows which agents fail on which checks for which clients, with filtering and detail views.

## Files Created
- `cms/apps/web/src/types/failure-patterns.ts` — TypeScript interfaces
- `cms/apps/web/src/hooks/use-failure-patterns.ts` — SWR hooks (useFailurePatterns, useFailurePatternStats)
- `cms/apps/web/src/app/(dashboard)/failure-patterns/page.tsx` — Main page
- `cms/apps/web/src/app/(dashboard)/failure-patterns/loading.tsx` — Skeleton loading
- `cms/apps/web/src/components/failure-patterns/stats-cards.tsx` — 4 stat overview cards
- `cms/apps/web/src/components/failure-patterns/filters.tsx` — Agent + QA check filter bar
- `cms/apps/web/src/components/failure-patterns/pattern-table.tsx` — Clickable pattern table
- `cms/apps/web/src/components/failure-patterns/detail-dialog.tsx` — 32rem detail dialog
- `cms/apps/web/src/lib/demo/data/failure-patterns.ts` — 7 demo patterns + stats

## Files Modified
- `app/ai/blueprints/schemas.py` — Added FailurePatternResponse, FailurePatternListResponse, FailurePatternStats
- `app/ai/blueprints/routes.py` — Added GET /failure-patterns and GET /failure-patterns/stats endpoints
- `cms/apps/web/src/lib/demo/resolver.ts` — Added failure pattern demo routes
- `cms/apps/web/middleware.ts` — Added /failure-patterns route permission
- `cms/apps/web/src/app/(dashboard)/layout.tsx` — Added sidebar nav item
- `cms/apps/web/messages/en.json` — Added failurePatterns i18n keys (33 keys)

## Security Checklist
- [x] No `(x as any)` type casts — proper type augmentation used
- [x] API calls use `authFetch` via SWR fetcher
- [x] No `dangerouslySetInnerHTML` — all text rendered safely
- [x] Token handling via existing auth infrastructure
- [x] Read-only endpoints (GET only), all roles allowed
- [x] Backend queries use SQLAlchemy ORM (no raw SQL injection)

## Verification
- [x] `npx tsc --noEmit` passes — zero TypeScript errors
- [x] Backend schemas import successfully
- [x] All user-visible text uses `useTranslations()`
- [x] Semantic Tailwind tokens only (no primitive colors)
- [x] Auth/RBAC: admin, developer, viewer roles allowed
- [x] No `as any` casts in changed files
