# Plan: Phase 25.15 — Tests for Phases 25.10–25.14

## Context

Phases 25.10–25.14 added five major features. Existing test count: ~100 tests across 8 files. Many modules have only happy-path coverage — extractors, builders, routes, error paths, and integration points are undertested. This plan adds 80+ tests to reach comprehensive coverage.

**Current test inventory:**
| Module | File | Existing Tests |
|--------|------|---------------|
| Template Upload (25.10) | `test_analyzer.py` | 19 |
| Template Upload (25.10) | `test_routes.py` | 5 |
| Template Upload (25.10) | `test_service.py` | 9 |
| Skill Extraction (25.11) | `test_extractor.py` | 20 |
| Skill Extraction (25.11) | `test_amendment.py` | 4 |
| Skill Extraction (25.11) | `test_routes.py` | 1 (stub) |
| Template Eval (25.12) | `test_template_eval_generator.py` | 15 |
| Deliverability Intel (25.13) | `test_deliverability_intel.py` | 19 |
| Variant Assembly (25.14) | `test_variant_generator.py` | 8 |

## Files to Create/Modify

### New test files
- `app/templates/upload/tests/test_template_builder.py` — GoldenTemplate construction (7 tests)
- `app/templates/upload/tests/test_knowledge_injector.py` — Knowledge injection (6 tests)
- `app/templates/upload/tests/test_extractors.py` — SlotExtractor + TokenExtractor (8 tests)

### Existing test files to extend
- `app/ai/skills/tests/test_amendment.py` — Add `apply_amendments` tests (6 tests)
- `app/ai/skills/tests/test_routes.py` — Replace stub with real route tests (6 tests)
- `app/ai/agents/evals/tests/test_template_eval_generator.py` — Add route + edge case tests (8 tests)
- `app/qa_engine/tests/test_deliverability_intel.py` — Add dimension scoring + ISP tests (16 tests)
- `app/ai/agents/scaffolder/tests/test_variant_generator.py` — Add strategy selection + service tests (10 tests)
- `app/templates/upload/tests/test_service.py` — Add eval/knowledge integration assertions (5 tests)
- `app/templates/upload/tests/test_routes.py` — Add upload flow route tests (4 tests)

### Source files — read only (no modifications)
- `app/templates/upload/template_builder.py`
- `app/templates/upload/knowledge_injector.py`
- `app/templates/upload/slot_extractor.py`
- `app/templates/upload/token_extractor.py`
- `app/ai/skills/amendment.py`
- `app/ai/skills/routes.py`
- `app/ai/skills/service.py`
- `app/ai/agents/evals/template_eval_routes.py`
- `app/qa_engine/deliverability_analyzer.py`
- `app/qa_engine/checks/deliverability.py`
- `app/ai/agents/scaffolder/variant_generator.py`
- `app/ai/agents/scaffolder/variant_routes.py`
- `app/ai/agents/scaffolder/service.py`

## Implementation Steps

### Step 1: Template Upload — SlotExtractor + TokenExtractor (8 tests)

**Create `app/templates/upload/tests/test_extractors.py`**

```python
"""Tests for slot and token extractors (Phase 25.10)."""
from __future__ import annotations
from app.templates.upload.slot_extractor import SlotExtractor
from app.templates.upload.token_extractor import TokenExtractor
from app.templates.upload.analyzer import SectionInfo, SlotInfo, TokenInfo


class TestSlotExtractor:
    def test_maps_headline_slot_type(self) -> None:
        """headline SlotInfo → headline TemplateSlot."""
        slots = [SlotInfo(slot_id="s1", slot_type="headline", selector="h1",
                         required=True, max_chars=100, content_preview="Hello",
                         section_id="hero")]
        sections = [SectionInfo(section_id="hero", component_name="hero",
                               element_count=5, layout_type="single")]
        result = SlotExtractor.extract(slots, sections)
        assert len(result) == 1
        assert result[0].slot_type == "headline"
        assert result[0].required is True

    def test_maps_body_and_image_types(self) -> None:
        """body and image slot types mapped correctly."""
        slots = [
            SlotInfo(slot_id="s1", slot_type="body", selector="p.body",
                    required=False, max_chars=500, content_preview="Text",
                    section_id="sec1"),
            SlotInfo(slot_id="s2", slot_type="image", selector="img.hero",
                    required=True, max_chars=None, content_preview="",
                    section_id="sec1"),
        ]
        sections = [SectionInfo(section_id="sec1", component_name="content",
                               element_count=3, layout_type="single")]
        result = SlotExtractor.extract(slots, sections)
        types = {s.slot_type for s in result}
        assert "body" in types
        assert "image" in types

    def test_empty_slots_returns_empty(self) -> None:
        result = SlotExtractor.extract([], [])
        assert result == ()

    def test_placeholder_truncation(self) -> None:
        """Content preview truncated to 60 chars max."""
        long_preview = "A" * 200
        slots = [SlotInfo(slot_id="s1", slot_type="body", selector="p",
                         required=False, max_chars=None, content_preview=long_preview,
                         section_id="sec")]
        sections = [SectionInfo(section_id="sec", component_name="body",
                               element_count=1, layout_type="single")]
        result = SlotExtractor.extract(slots, sections)
        assert len(result[0].placeholder) <= 60


class TestTokenExtractor:
    def test_color_role_assignment(self) -> None:
        """Most common bg → background role, text → text role."""
        info = TokenInfo(
            colors={"background-color": ["#ffffff", "#ffffff", "#f5f5f5"],
                    "color": ["#333333", "#333333", "#0066cc"]},
            fonts={"font-family": ["Arial", "Georgia"]},
            font_sizes={"font-size": ["16px", "24px", "12px"]},
            spacing={"padding": ["20px", "20px", "10px"]},
        )
        tokens = TokenExtractor.extract(info)
        assert tokens.background is not None
        assert tokens.text is not None

    def test_empty_token_info(self) -> None:
        """Empty TokenInfo → DefaultTokens with all None fields."""
        info = TokenInfo(colors={}, fonts={}, font_sizes={}, spacing={})
        tokens = TokenExtractor.extract(info)
        # Should not raise; fields may be None or empty
        assert tokens is not None

    def test_font_size_sorting(self) -> None:
        """Largest → heading, 2nd → body, smallest → small."""
        info = TokenInfo(
            colors={},
            fonts={"font-family": ["Helvetica"]},
            font_sizes={"font-size": ["32px", "16px", "12px"]},
            spacing={},
        )
        tokens = TokenExtractor.extract(info)
        if tokens.heading_font_size and tokens.body_font_size:
            h = int(tokens.heading_font_size.replace("px", ""))
            b = int(tokens.body_font_size.replace("px", ""))
            assert h > b

    def test_spacing_extraction(self) -> None:
        """Most common padding → section spacing."""
        info = TokenInfo(
            colors={},
            fonts={},
            font_sizes={},
            spacing={"padding": ["24px", "24px", "24px", "8px"]},
        )
        tokens = TokenExtractor.extract(info)
        if tokens.section_spacing:
            assert "24" in tokens.section_spacing
```

### Step 2: Template Upload — TemplateBuilder (7 tests)

**Create `app/templates/upload/tests/test_template_builder.py`**

```python
"""Tests for GoldenTemplate builder (Phase 25.10)."""
from __future__ import annotations
from app.templates.upload.template_builder import TemplateBuilder
from app.ai.templates.models import DefaultTokens, TemplateSlot


class TestTemplateBuilder:
    def _make_slots(self) -> tuple[TemplateSlot, ...]:
        return (
            TemplateSlot(slot_id="hero_heading", slot_type="headline",
                        selector="h1", required=True, placeholder="Heading"),
            TemplateSlot(slot_id="hero_image", slot_type="image",
                        selector="img.hero", required=True, placeholder=""),
            TemplateSlot(slot_id="body_text", slot_type="body",
                        selector="p.body", required=False, placeholder="Body"),
        )

    def _make_tokens(self) -> DefaultTokens:
        return DefaultTokens(
            background="#ffffff", text="#333333", cta="#0066cc",
            heading_font="Arial", body_font="Georgia",
        )

    def test_uploaded_prefix(self) -> None:
        """Template name gets 'uploaded_' prefix."""
        tmpl = TemplateBuilder.build(
            sanitized_html="<html><body>Test</body></html>",
            slots=self._make_slots(), tokens=self._make_tokens(),
            layout_type="promotional", column_count=1,
            sections=["hero"], name="my_promo",
        )
        assert tmpl.metadata.name.startswith("uploaded_")

    def test_source_is_uploaded(self) -> None:
        tmpl = TemplateBuilder.build(
            sanitized_html="<html><body>Test</body></html>",
            slots=self._make_slots(), tokens=self._make_tokens(),
            layout_type="newsletter", column_count=2,
            sections=["hero", "body"],
        )
        assert tmpl.metadata.source == "uploaded"

    def test_auto_name_generation(self) -> None:
        """When name=None, generates hash-based name."""
        tmpl = TemplateBuilder.build(
            sanitized_html="<html><body>Auto named</body></html>",
            slots=self._make_slots(), tokens=self._make_tokens(),
            layout_type="transactional", column_count=1, sections=["hero"],
        )
        assert tmpl.metadata.name.startswith("uploaded_")
        assert len(tmpl.metadata.name) > len("uploaded_")

    def test_hero_image_detection(self) -> None:
        """Required image slot → has_hero_image=True."""
        tmpl = TemplateBuilder.build(
            sanitized_html="<html><body>Hero</body></html>",
            slots=self._make_slots(), tokens=self._make_tokens(),
            layout_type="promotional", column_count=1, sections=["hero"],
        )
        assert tmpl.metadata.has_hero_image is True

    def test_no_hero_without_image_slot(self) -> None:
        """No required image slot → has_hero_image=False."""
        slots = (
            TemplateSlot(slot_id="heading", slot_type="headline",
                        selector="h1", required=True, placeholder="Hi"),
        )
        tmpl = TemplateBuilder.build(
            sanitized_html="<html><body>No hero</body></html>",
            slots=slots, tokens=self._make_tokens(),
            layout_type="transactional", column_count=1, sections=["main"],
        )
        assert tmpl.metadata.has_hero_image is False

    def test_empty_slots_allowed(self) -> None:
        """Builder handles templates with no detected slots."""
        tmpl = TemplateBuilder.build(
            sanitized_html="<html><body>Minimal</body></html>",
            slots=(), tokens=self._make_tokens(),
            layout_type="minimal", column_count=1, sections=["body"],
        )
        assert len(tmpl.slot_definitions) == 0

    def test_description_override(self) -> None:
        """Custom description is preserved."""
        tmpl = TemplateBuilder.build(
            sanitized_html="<html><body>Desc</body></html>",
            slots=self._make_slots(), tokens=self._make_tokens(),
            layout_type="promotional", column_count=1,
            sections=["hero"], description="My custom description",
        )
        assert tmpl.metadata.description == "My custom description"
```

### Step 3: Template Upload — KnowledgeInjector (6 tests)

**Create `app/templates/upload/tests/test_knowledge_injector.py`**

```python
"""Tests for knowledge injection from uploaded templates (Phase 25.10)."""
from __future__ import annotations
from unittest.mock import AsyncMock, MagicMock, patch

import pytest

from app.templates.upload.knowledge_injector import KnowledgeInjector
from app.templates.upload.analyzer import AnalysisResult, ComplexityInfo, SectionInfo, TokenInfo


def _make_analysis(**overrides: object) -> AnalysisResult:
    defaults = dict(
        sections=[SectionInfo(section_id="s1", component_name="hero",
                             element_count=5, layout_type="single")],
        slots=[],
        tokens=TokenInfo(colors={}, fonts={}, font_sizes={}, spacing={}),
        esp_platform=None,
        complexity=ComplexityInfo(column_count=1, nesting_depth=2,
                                mso_conditional_count=0, total_elements=10,
                                table_nesting_depth=1, has_vml=False, has_amp=False),
        layout_type="promotional",
    )
    defaults.update(overrides)
    return AnalysisResult(**defaults)  # type: ignore[arg-type]


class TestKnowledgeInjector:
    @pytest.mark.asyncio
    async def test_injects_vml_pattern(self) -> None:
        """VML HTML triggers knowledge injection."""
        mock_svc = AsyncMock()
        mock_svc.ingest_document.return_value = MagicMock(id=42)
        injector = KnowledgeInjector(mock_svc)
        html = '<html><body><v:rect style="width:200px"><v:fill type="frame" src="bg.jpg"/></v:rect></body></html>'
        result = await injector.inject("test_tmpl", html, _make_analysis(), None)
        assert result is not None
        mock_svc.ingest_document.assert_called_once()

    @pytest.mark.asyncio
    async def test_injects_mso_conditional_pattern(self) -> None:
        """MSO conditional HTML triggers injection."""
        mock_svc = AsyncMock()
        mock_svc.ingest_document.return_value = MagicMock(id=43)
        injector = KnowledgeInjector(mock_svc)
        html = '<html><body><!--[if mso]><table><tr><td><![endif]-->Content<!--[if mso]></td></tr></table><![endif]--></body></html>'
        result = await injector.inject("test_tmpl", html, _make_analysis(), None)
        assert result is not None

    @pytest.mark.asyncio
    async def test_no_patterns_returns_none(self) -> None:
        """Plain HTML without patterns → None (no injection)."""
        mock_svc = AsyncMock()
        injector = KnowledgeInjector(mock_svc)
        html = "<html><body><p>Simple text email.</p></body></html>"
        result = await injector.inject("test_tmpl", html, _make_analysis(), None)
        assert result is None
        mock_svc.ingest_document.assert_not_called()

    @pytest.mark.asyncio
    async def test_exception_returns_none(self) -> None:
        """Knowledge service failure → None (non-blocking)."""
        mock_svc = AsyncMock()
        mock_svc.ingest_document.side_effect = RuntimeError("DB down")
        injector = KnowledgeInjector(mock_svc)
        html = '<html><body><v:rect>VML</v:rect></body></html>'
        result = await injector.inject("test_tmpl", html, _make_analysis(), None)
        assert result is None

    @pytest.mark.asyncio
    async def test_dark_mode_pattern_detected(self) -> None:
        """prefers-color-scheme media query triggers injection."""
        mock_svc = AsyncMock()
        mock_svc.ingest_document.return_value = MagicMock(id=44)
        injector = KnowledgeInjector(mock_svc)
        html = '<html><head><style>@media (prefers-color-scheme: dark) { .dark { color: #fff; } }</style></head><body><p>Dark</p></body></html>'
        result = await injector.inject("test_tmpl", html, _make_analysis(), None)
        assert result is not None

    @pytest.mark.asyncio
    async def test_esp_platform_included(self) -> None:
        """ESP platform info is included in knowledge document."""
        mock_svc = AsyncMock()
        mock_svc.ingest_document.return_value = MagicMock(id=45)
        injector = KnowledgeInjector(mock_svc)
        html = '<html><body>{% if user.premium %}<v:rect>VML</v:rect>{% endif %}</body></html>'
        result = await injector.inject("test_tmpl", html, _make_analysis(), "braze")
        assert result is not None
        call_args = mock_svc.ingest_document.call_args
        # Verify ESP platform mentioned in the document content
        content = str(call_args)
        assert "braze" in content.lower() or result is not None
```

### Step 4: Template Upload — Service Integration (5 tests)

**Append to `app/templates/upload/tests/test_service.py`**

Add tests verifying eval generation + knowledge injection integration during confirm:

```python
# Tests to add:
# 1. test_confirm_triggers_eval_generation — mock _eval_gen.generate, verify called with GoldenTemplate
# 2. test_confirm_triggers_knowledge_injection — mock _knowledge_injector.inject, verify called
# 3. test_confirm_eval_failure_non_blocking — _eval_gen.generate raises → confirm still succeeds
# 4. test_confirm_knowledge_failure_non_blocking — _knowledge_injector.inject raises → confirm still succeeds
# 5. test_confirm_with_overrides — name/description overrides applied to GoldenTemplate
```

### Step 5: Template Upload — Route Tests (4 tests)

**Append to `app/templates/upload/tests/test_routes.py`**

```python
# Tests to add:
# 1. test_upload_rejects_oversized_file — >2MB → 400
# 2. test_upload_requires_auth — no token → 401
# 3. test_confirm_wrong_user — other user's upload → 404
# 4. test_reject_nonexistent — missing upload → 404
```

### Step 6: Skill Extraction — Amendment `apply_amendments` (6 tests)

**Append to `app/ai/skills/tests/test_amendment.py`**

```python
# Import apply_amendments + AmendmentReport
from app.ai.skills.amendment import apply_amendments
from app.ai.skills.schemas import SkillAmendment, AmendmentReport

class TestApplyAmendments:
    def test_dry_run_generates_diffs(self) -> None:
        """dry_run=True produces diffs without writing files."""
        amendment = SkillAmendment(
            id="test1", agent_name="outlook_fixer",
            skill_file="skills/vml_reference.md",
            section="## Patterns", content="New VML pattern",
            confidence=0.9, source_pattern_id="p1",
        )
        report = apply_amendments([amendment], dry_run=True)
        assert isinstance(report, AmendmentReport)
        assert report.applied == 0  # dry run doesn't apply

    def test_path_traversal_blocked(self) -> None:
        """Skill file path outside app/ai/agents/ → skipped."""
        amendment = SkillAmendment(
            id="evil", agent_name="outlook_fixer",
            skill_file="../../etc/passwd",
            section="## Exploit", content="malicious",
            confidence=0.95, source_pattern_id="p2",
        )
        report = apply_amendments([amendment], dry_run=True)
        assert report.applied == 0

    def test_duplicate_detection(self) -> None:
        """Content already in skill file → skipped_duplicate."""
        # This test mocks the skill file content to contain the amendment text
        # and verifies it's counted as duplicate
        pass  # Implementation reads actual skill file; mock Path.read_text

    def test_conflict_detection(self) -> None:
        """Conflicting content in same section → skipped_conflict."""
        pass  # Verify conflicting amendments counted correctly

    def test_empty_amendments_list(self) -> None:
        """Empty list → report with all zeros."""
        report = apply_amendments([], dry_run=True)
        assert report.total == 0
        assert report.applied == 0

    def test_report_diff_format(self) -> None:
        """Diffs are unified diff format strings."""
        amendment = SkillAmendment(
            id="test2", agent_name="scaffolder",
            skill_file="skills/email_structure.md",
            section="## Layout", content="Grid fallback pattern",
            confidence=0.85, source_pattern_id="p3",
        )
        report = apply_amendments([amendment], dry_run=True)
        # Diffs should be present for non-skipped amendments
        assert isinstance(report.diffs, list)
```

### Step 7: Skill Extraction — Route Tests (6 tests)

**Replace stub in `app/ai/skills/tests/test_routes.py`**

```python
"""Route tests for skill extraction API endpoints."""
from unittest.mock import AsyncMock, MagicMock, patch
import pytest
from httpx import AsyncClient, ASGITransport
from app.core.rate_limit import limiter
from app.main import app


@pytest.fixture(autouse=True)
def _disable_rate_limit() -> None:
    limiter.enabled = False


@pytest.fixture()
def client() -> AsyncClient:
    return AsyncClient(transport=ASGITransport(app=app), base_url="http://test")


class TestSkillRoutes:
    @pytest.mark.asyncio
    async def test_list_pending_requires_auth(self, client: AsyncClient) -> None:
        """No auth token → 401/403."""
        resp = await client.get("/api/v1/skills/amendments/pending")
        assert resp.status_code in (401, 403)

    @pytest.mark.asyncio
    @patch("app.ai.skills.service.SkillExtractionService.list_pending")
    async def test_list_pending_with_agent_filter(self, mock_list: AsyncMock, client: AsyncClient) -> None:
        """agent_name query param filters results."""
        mock_list.return_value = ([], 0)
        # Would need auth mock — pattern: mock get_current_user dependency
        pass

    @pytest.mark.asyncio
    async def test_approve_requires_admin(self, client: AsyncClient) -> None:
        """Non-admin user → 403."""
        pass  # Mock user with developer role, verify 403

    @pytest.mark.asyncio
    async def test_reject_nonexistent_returns_404(self, client: AsyncClient) -> None:
        """Unknown amendment_id → 404."""
        pass

    @pytest.mark.asyncio
    async def test_batch_empty_actions(self, client: AsyncClient) -> None:
        """Empty actions list → validated (0 processed)."""
        pass

    @pytest.mark.asyncio
    async def test_batch_mixed_actions(self, client: AsyncClient) -> None:
        """Mix of approve + reject in one batch."""
        pass
```

### Step 8: Template Eval — Route + Edge Case Tests (8 tests)

**Append to `app/ai/agents/evals/tests/test_template_eval_generator.py`**

```python
# Tests to add at bottom of file:

class TestTemplateEvalRoutes:
    """Route-level tests for /api/v1/evals/templates endpoints."""

    @pytest.mark.asyncio
    async def test_list_requires_auth(self) -> None:
        """GET /api/v1/evals/templates without auth → 401/403."""
        pass

    @pytest.mark.asyncio
    async def test_get_cases_for_unknown_template(self) -> None:
        """GET /api/v1/evals/templates/nonexistent/cases → 404."""
        pass

    @pytest.mark.asyncio
    async def test_delete_requires_admin(self) -> None:
        """DELETE with developer role → 403 (admin only)."""
        pass

    @pytest.mark.asyncio
    async def test_delete_returns_204(self) -> None:
        """DELETE existing template cases → 204."""
        pass


class TestEdgeCases:
    def test_zero_slots_template(self) -> None:
        """Template with no slots → slot_fill case has expected_count=0."""
        # Build GoldenTemplate with empty slot_definitions
        pass

    def test_unknown_layout_fallback(self) -> None:
        """Unknown layout type → falls back to newsletter brief."""
        pass

    def test_load_case_set_returns_metadata(self) -> None:
        """load_case_set() returns dict with 'cases' and 'generated_at' keys."""
        pass

    def test_qa_passthrough_detects_features(self) -> None:
        """QA passthrough case detects accessibility/MSO/dark_mode features."""
        pass
```

### Step 9: Deliverability Intelligence — Dimension Scoring + ISP Tests (16 tests)

**Append to `app/qa_engine/tests/test_deliverability_intel.py`**

```python
# --- Dimension scoring tests (checks/deliverability.py internals) ---

class TestContentQualityScoring:
    """Test _score_content_quality dimension."""

    def test_high_image_ratio_penalized(self) -> None:
        """Image-heavy email (>60% image area) → content quality deduction."""
        html = '''<!DOCTYPE html><html><body>
            <img src="a.jpg" width="600" height="400">
            <img src="b.jpg" width="600" height="300">
            <p>Short</p>
        </body></html>'''
        score, _, dims, _ = get_detailed_result(html)
        content_dim = next(d for d in dims if "content" in d.name.lower())
        assert content_dim.score < 25  # Heavy penalty for image-dominated

    def test_url_shorteners_penalized(self) -> None:
        """bit.ly/t.co links → content quality deduction."""
        html = '''<!DOCTYPE html><html><body>
            <p>Check out our amazing deals on products today.</p>
            <a href="https://bit.ly/xyz">Click here</a>
            <a href="https://t.co/abc">Learn more</a>
        </body></html>'''
        score, _, dims, _ = get_detailed_result(html)
        content_dim = next(d for d in dims if "content" in d.name.lower())
        assert content_dim.score < 25

    def test_balanced_content_scores_well(self) -> None:
        """Good text-to-image ratio → high content quality score."""
        html = '''<!DOCTYPE html><html><head><meta charset="utf-8"></head><body>
            <p>We are excited to share our latest product updates with you today.
            Our team has been working hard to improve the user experience and we
            believe these changes will make a real difference in your workflow.</p>
            <img src="https://cdn.example.com/logo.png" alt="Logo" width="200">
            <p>Here are the key improvements we have made this quarter.</p>
            <a href="https://example.com/details">Learn More</a>
        </body></html>'''
        score, _, dims, _ = get_detailed_result(html)
        content_dim = next(d for d in dims if "content" in d.name.lower())
        assert content_dim.score >= 20


class TestHtmlHygieneScoring:
    """Test _score_html_hygiene dimension."""

    def test_missing_doctype_penalized(self) -> None:
        """No DOCTYPE → hygiene deduction."""
        html = '<html><body><p>No doctype.</p></body></html>'
        _, _, dims, _ = get_detailed_result(html)
        hygiene_dim = next(d for d in dims if "hygiene" in d.name.lower())
        assert hygiene_dim.score < 25

    def test_missing_charset_penalized(self) -> None:
        """No charset meta → hygiene deduction."""
        html = '<!DOCTYPE html><html><body><p>No charset.</p></body></html>'
        _, _, dims, _ = get_detailed_result(html)
        hygiene_dim = next(d for d in dims if "hygiene" in d.name.lower())
        assert hygiene_dim.score < 25

    def test_hidden_text_penalized(self) -> None:
        """Hidden text via display:none (non-preheader) → hygiene deduction."""
        html = '''<!DOCTYPE html><html><head><meta charset="utf-8"></head><body>
            <div style="display:none">Hidden spam text keywords</div>
            <p>Visible content here.</p>
        </body></html>'''
        _, _, dims, _ = get_detailed_result(html)
        hygiene_dim = next(d for d in dims if "hygiene" in d.name.lower())
        assert hygiene_dim.score < 25


class TestAuthReadinessScoring:
    """Test _score_auth_readiness dimension."""

    def test_full_auth_readiness(self) -> None:
        """Unsubscribe link + address + List-Unsubscribe → high score."""
        html = '''<!DOCTYPE html><html><head><meta charset="utf-8"></head><body>
            <p>Content here.</p>
            <footer class="footer">
                <p>123 Main Street, Suite 100, City, ST 12345</p>
                <a href="https://example.com/unsubscribe">Unsubscribe</a>
                <!-- List-Unsubscribe: <mailto:unsub@example.com> -->
            </footer>
        </body></html>'''
        _, _, dims, _ = get_detailed_result(html)
        auth_dim = next(d for d in dims if "auth" in d.name.lower())
        assert auth_dim.score >= 20

    def test_missing_unsubscribe(self) -> None:
        """No unsubscribe link → auth readiness deduction."""
        html = '''<!DOCTYPE html><html><head><meta charset="utf-8"></head><body>
            <p>Content without unsubscribe.</p>
            <footer class="footer"><p>123 Main St, City, ST 12345</p></footer>
        </body></html>'''
        _, _, dims, _ = get_detailed_result(html)
        auth_dim = next(d for d in dims if "auth" in d.name.lower())
        assert auth_dim.score < 25


class TestEngagementSignals:
    """Test _score_engagement_signals dimension."""

    def test_preview_text_boosts_score(self) -> None:
        """Preheader/preview text → engagement signal bonus."""
        html = '''<!DOCTYPE html><html><head><meta charset="utf-8"></head><body>
            <div class="preheader" style="display:none;max-height:0">Preview text for inbox</div>
            <p>Main content with enough words for engagement scoring to work properly.</p>
            <a href="https://example.com/cta" style="background:#0066cc;color:#fff;padding:12px">Shop Now</a>
        </body></html>'''
        _, _, dims, _ = get_detailed_result(html)
        eng_dim = next(d for d in dims if "engagement" in d.name.lower())
        assert eng_dim.score >= 15

    def test_personalization_tokens_boost(self) -> None:
        """{{first_name}} merge tag → engagement bonus."""
        html = '''<!DOCTYPE html><html><head><meta charset="utf-8"></head><body>
            <p>Hello {{first_name}}, we have exciting news for you today.</p>
            <p>As a valued customer, we wanted to share these updates.</p>
        </body></html>'''
        _, _, dims, _ = get_detailed_result(html)
        eng_dim = next(d for d in dims if "engagement" in d.name.lower())
        assert eng_dim.score >= 10


class TestISPSpecificAnalysis:
    """Isolated ISP-specific analyzer tests."""

    def test_gmail_promo_tab_coupon_trigger(self) -> None:
        """Coupon code pattern triggers Gmail promo tab flag."""
        html = '''<!DOCTYPE html><html><body>
            <p>Use code SUMMER2026 for 25% off your order!</p>
        </body></html>'''
        result = analyze(html)
        assert result.gmail_promo_tab_score >= 1

    def test_microsoft_link_count_threshold(self) -> None:
        """15+ links trigger Microsoft SmartScreen flag."""
        links = "\n".join(f'<a href="https://example.com/link{i}">Link {i}</a>' for i in range(16))
        html = f'<!DOCTYPE html><html><body><p>Content</p>{links}</body></html>'
        result = analyze(html)
        ms = result.isp_risks.get("microsoft")
        assert ms is not None
        assert any("link" in f.description.lower() for f in ms.flags)

    def test_yahoo_address_detection(self) -> None:
        """Physical address in footer satisfies Yahoo requirement."""
        html = '''<!DOCTYPE html><html><body>
            <p>Content here.</p>
            <footer class="footer">
                <p>123 Main Street, Suite 500, New York, NY 10001</p>
                <a href="https://example.com/unsub">Unsubscribe</a>
            </footer>
        </body></html>'''
        result = analyze(html)
        yahoo = result.isp_risks.get("yahoo")
        if yahoo:
            address_flags = [f for f in yahoo.flags if "address" in f.description.lower()]
            assert len(address_flags) == 0  # No address violation

    def test_tracking_params_flagged(self) -> None:
        """URLs with >8 query params → structural flag."""
        params = "&".join(f"p{i}=v{i}" for i in range(10))
        html = f'<!DOCTYPE html><html><body><a href="https://example.com/track?{params}">Click</a></body></html>'
        result = analyze(html)
        assert any("param" in sf.lower() or "tracking" in sf.lower() for sf in result.structural_flags)
```

### Step 10: Variant Assembly — Strategy + Service Tests (10 tests)

**Append to `app/ai/agents/scaffolder/tests/test_variant_generator.py`**

```python
from unittest.mock import AsyncMock, MagicMock, patch
import pytest
from app.ai.agents.scaffolder.variant_generator import select_strategies
from app.ai.agents.scaffolder.variant_schemas import (
    CampaignVariantSet, VariantPlan, VariantResult,
)


class TestSelectStrategies:
    """Test LLM strategy selection with mocked call_json."""

    @pytest.mark.asyncio
    async def test_returns_requested_count(self) -> None:
        """select_strategies returns exactly N strategies."""
        mock_call = AsyncMock(return_value={
            "strategies": [
                {"name": "urgency_driven", "hypothesis": "H1", "differentiator": "D1"},
                {"name": "benefit_focused", "hypothesis": "H2", "differentiator": "D2"},
                {"name": "social_proof", "hypothesis": "H3", "differentiator": "D3"},
            ]
        })
        result = await select_strategies("Summer sale campaign", 3, mock_call)
        assert len(result) == 3
        assert result[0][0] == "urgency_driven"

    @pytest.mark.asyncio
    async def test_handles_fewer_than_requested(self) -> None:
        """LLM returns fewer strategies → returns what's available."""
        mock_call = AsyncMock(return_value={
            "strategies": [
                {"name": "urgency_driven", "hypothesis": "H1", "differentiator": "D1"},
            ]
        })
        result = await select_strategies("Brief", 3, mock_call)
        assert len(result) >= 1


class TestVariantPlanConstruction:
    """Test VariantPlan frozen dataclass."""

    def test_variant_plan_immutable(self) -> None:
        """VariantPlan is frozen — can't mutate fields."""
        plan = VariantPlan(
            variant_id="A", strategy_name="urgency_driven",
            hypothesis="H", slot_fills=(SlotFill(slot_id="cta", content="Buy"),),
            subject_line="Subj", preheader="Pre",
            predicted_differentiator="Diff",
        )
        with pytest.raises(AttributeError):
            plan.variant_id = "B"  # type: ignore[misc]


class TestVariantResult:
    """Test VariantResult data structure."""

    def test_qa_passed_true_when_all_pass(self) -> None:
        """qa_passed=True when all QA checks pass."""
        result = VariantResult(
            variant_id="A", strategy_name="urgency_driven",
            hypothesis="H", predicted_differentiator="D",
            subject_line="S", preheader="P", html="<html></html>",
            build_plan=None, qa_results={"html": True, "css": True},
            qa_passed=True,
        )
        assert result.qa_passed is True

    def test_qa_passed_false_when_any_fail(self) -> None:
        result = VariantResult(
            variant_id="B", strategy_name="benefit_focused",
            hypothesis="H", predicted_differentiator="D",
            subject_line="S", preheader="P", html="<html></html>",
            build_plan=None, qa_results={"html": True, "css": False},
            qa_passed=False,
        )
        assert result.qa_passed is False


class TestCampaignVariantSet:
    """Test CampaignVariantSet top-level structure."""

    def test_set_contains_comparison(self) -> None:
        """CampaignVariantSet includes comparison matrix."""
        from app.ai.agents.scaffolder.variant_generator import build_comparison_matrix
        v1 = VariantPlan(
            variant_id="A", strategy_name="urgency_driven",
            hypothesis="H1", slot_fills=(SlotFill(slot_id="cta", content="Buy now"),),
            subject_line="Last chance!", preheader="Don't miss out",
            predicted_differentiator="Urgency",
        )
        v2 = VariantPlan(
            variant_id="B", strategy_name="benefit_focused",
            hypothesis="H2", slot_fills=(SlotFill(slot_id="cta", content="Save more"),),
            subject_line="Save 50%", preheader="Great deals",
            predicted_differentiator="Value",
        )
        matrix = build_comparison_matrix([v1, v2])
        variant_set = CampaignVariantSet(
            brief="Test brief", base_template="promo_1col",
            base_design_tokens={}, variants=[], comparison=matrix,
        )
        assert variant_set.comparison is not None
        assert len(variant_set.comparison.subject_lines) == 2


class TestVariantServiceValidation:
    """Test ScaffolderService.generate_variants config validation."""

    @pytest.mark.asyncio
    @patch("app.ai.agents.scaffolder.service.get_settings")
    async def test_disabled_setting_raises(self, mock_settings: MagicMock) -> None:
        """Variants disabled in settings → AIExecutionError."""
        from app.ai.agents.scaffolder.service import ScaffolderService
        from app.ai.agents.scaffolder.schemas import VariantRequest
        mock_settings.return_value.variants = MagicMock(enabled=False, max_variants=5)
        service = ScaffolderService.__new__(ScaffolderService)
        service._settings = mock_settings.return_value
        with pytest.raises(Exception):  # AIExecutionError or similar
            await service.generate_variants(VariantRequest(brief="Test brief", variant_count=3))

    @pytest.mark.asyncio
    @patch("app.ai.agents.scaffolder.service.get_settings")
    async def test_exceeds_max_raises(self, mock_settings: MagicMock) -> None:
        """variant_count > max_variants → error."""
        from app.ai.agents.scaffolder.service import ScaffolderService
        from app.ai.agents.scaffolder.schemas import VariantRequest
        mock_settings.return_value.variants = MagicMock(enabled=True, max_variants=3)
        service = ScaffolderService.__new__(ScaffolderService)
        service._settings = mock_settings.return_value
        with pytest.raises(Exception):
            await service.generate_variants(VariantRequest(brief="Test brief", variant_count=5))
```

### Step 11: Run Tests and Fix

After writing all test files, run the full test suite:

```bash
# Run just the new/modified test files first
pytest app/templates/upload/tests/ app/ai/skills/tests/ app/ai/agents/evals/tests/test_template_eval_generator.py app/qa_engine/tests/test_deliverability_intel.py app/ai/agents/scaffolder/tests/test_variant_generator.py -v --tb=short 2>&1 | tail -50

# Fix any import errors, missing fixtures, or assertion failures

# Then run full suite
make test

# Then full check
make check
```

### Step 12: Final Verification

```bash
make eval-golden  # No regression in eval pipeline
```

## Test Count Summary

| Module | File | New Tests | Total |
|--------|------|-----------|-------|
| Template Upload | `test_extractors.py` (NEW) | 8 | 8 |
| Template Upload | `test_template_builder.py` (NEW) | 7 | 7 |
| Template Upload | `test_knowledge_injector.py` (NEW) | 6 | 6 |
| Template Upload | `test_service.py` | 5 | 14 |
| Template Upload | `test_routes.py` | 4 | 9 |
| Skill Extraction | `test_amendment.py` | 6 | 10 |
| Skill Extraction | `test_routes.py` | 6 | 6 |
| Template Eval | `test_template_eval_generator.py` | 8 | 23 |
| Deliverability | `test_deliverability_intel.py` | 16 | 35 |
| Variant Assembly | `test_variant_generator.py` | 10 | 18 |
| **Total** | | **76** | **136** |

> Note: Some tests marked `pass` need implementation details filled in during execution based on actual module APIs. The executor agent should read the source file before writing each test to match exact signatures, return types, and error classes.

## Security Checklist (scoped to this feature's endpoints)
N/A — This phase adds only tests, no new endpoints or routes.

## Verification
- [ ] `make test` passes (all 136+ tests across the 5 modules)
- [ ] `make check` passes (lint, types, tests, security)
- [ ] `make eval-golden` passes (no regression)
- [ ] No test files import secrets or credentials
- [ ] Tests use `tmp_path` for file I/O isolation (no stale artifacts)
- [ ] Rate limiter disabled in all route tests (`limiter.enabled = False`)
