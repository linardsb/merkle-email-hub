# Plan: Phase 18.5 — Tests & Documentation

## Context

Phases 18.1–18.3 shipped chaos engine, property-based testing, resilience check, and knowledge feedback. Existing test coverage is already substantial (~96 tests across 7 files), but gaps remain in:
1. **Chaos engine edge cases** — no test for profile-level failure details, empty HTML, score boundary conditions
2. **Knowledge writer** — no test for `_title_exists` SQL path, `_sanitize_markdown`, or ingestion failure handling
3. **Resilience check** — no test for severe error severity (score < 0.4), or config passthrough
4. **Property testing generators** — no test for `random_email_config()` seed reproducibility or edge configs
5. **Route tests** — missing chaos route error forwarding, property test unknown-invariant route error
6. **Service layer** — no direct tests for `run_chaos_test` auto-documentation flow or `run_property_test` config fallback
7. **ADR-007** — no architecture decision record for the resilience testing subsystem

Target: 35+ **new** tests + ADR-007.

## Files to Create/Modify

- `app/qa_engine/tests/test_chaos_engine.py` — Add engine edge case tests
- `app/qa_engine/tests/test_chaos_profiles.py` — Add profile edge case tests
- `app/qa_engine/tests/test_knowledge_writer.py` — Add writer edge case tests
- `app/qa_engine/tests/test_resilience_check.py` — Add severity + config tests
- `app/qa_engine/property_testing/tests/test_invariants.py` — Add invariant edge cases
- `app/qa_engine/property_testing/tests/test_generators.py` — Add `random_email_config` tests
- `app/qa_engine/property_testing/tests/test_runner.py` — Add route + runner edge cases
- `app/qa_engine/tests/test_service_chaos.py` — **New file**: service-layer tests for chaos + property test flows
- `docs/ARCHITECTURE.md` — Append ADR-007: Rendering Resilience Testing

## Implementation Steps

### Step 1: Service-layer tests — `app/qa_engine/tests/test_service_chaos.py` (new, ~12 tests)

Create a new test file focused on the service layer for chaos and property testing:

```python
# pyright: reportUnknownParameterType=false, reportMissingParameterType=false, reportUnknownMemberType=false, reportUnknownVariableType=false, reportUnknownArgumentType=false, reportCallIssue=false
"""Service-layer tests for chaos engine and property testing."""

from __future__ import annotations

from unittest.mock import AsyncMock, MagicMock, patch

import pytest

from app.qa_engine.schemas import ChaosTestRequest, PropertyTestRequest
from app.qa_engine.service import QAEngineService


def _make_service() -> QAEngineService:
    db = AsyncMock()
    return QAEngineService(db)


def _make_user(role: str = "developer") -> MagicMock:
    user = MagicMock()
    user.id = 1
    user.role = role
    return user
```

**Tests to write:**

1. `test_run_chaos_test_disabled_raises_forbidden` — Patch `get_settings` so `qa_chaos.enabled = False`, assert `ForbiddenError`
2. `test_run_chaos_test_returns_response` — Patch `get_settings` enabled=True, patch `ChaosEngine.run_chaos_test`, verify response mapping
3. `test_run_chaos_test_auto_document_on_critical_failures` — Enable `auto_document=True`, provide `project_id` + `user`, mock `ChaosEngine` to return critical failures, mock `ProjectService.verify_project_access`, mock `ChaosKnowledgeWriter.write_failure_documents`, assert writer called
4. `test_run_chaos_test_auto_document_skipped_no_project_id` — Same but no `project_id`, assert writer NOT called
5. `test_run_chaos_test_auto_document_skipped_no_user` — Same but no `user`, assert writer NOT called
6. `test_run_chaos_test_auto_document_failure_non_blocking` — Mock writer to raise, assert chaos response still returned (non-blocking)
7. `test_run_property_test_disabled_raises_forbidden` — Patch `get_settings` so `qa_property_testing.enabled = False`, assert `ForbiddenError`
8. `test_run_property_test_returns_response` — Enable property testing, patch runner, verify response
9. `test_run_property_test_config_fallback_seed` — Don't pass seed in request, verify config default used
10. `test_run_property_test_config_fallback_num_cases` — Don't pass num_cases in request, verify config default used
11. `test_run_property_test_unknown_invariant_propagates` — Runner raises `DomainValidationError`, assert it propagates
12. `test_run_checks_includes_resilience_when_enabled` — Enable both `qa_chaos.enabled` and `resilience_check_enabled`, verify resilience check appended to results

### Step 2: Chaos engine edge cases — add to `test_chaos_engine.py` (~5 tests)

Add to `TestChaosEngine` class:

1. `test_engine_profile_results_contain_check_counts` — Run with known profiles, assert each `profile_result` has `checks_passed + checks_failed == checks_total` (verify the accounting)
2. `test_engine_resilience_score_capped_at_one` — Run with very simple HTML that barely degrades, assert `resilience_score <= 1.0`
3. `test_engine_profile_results_have_descriptions` — Assert each profile result has non-empty `description` field
4. `test_engine_single_profile` — Run with exactly 1 profile, verify `profiles_tested == 1` and score is between 0 and 1
5. `test_engine_all_known_profiles` — Run with all 8 profile names, verify `profiles_tested == 8`

### Step 3: Chaos profile edge cases — add to `test_chaos_profiles.py` (~4 tests)

1. `test_outlook_word_engine_strips_var_references` — HTML with `var(--x)`, assert removed
2. `test_gmail_clipping_exact_boundary` — HTML exactly at 102,400 bytes, assert unchanged
3. `test_image_blocked_no_images_unchanged` — HTML with no `<img>`, assert output unchanged
4. `test_class_strip_preserves_other_attributes` — HTML with `id`, `style`, `class` attrs, assert only `class` removed

### Step 4: Knowledge writer edge cases — add to `test_knowledge_writer.py` (~4 tests)

1. `test_sanitize_markdown_strips_special_chars` — Call `_sanitize_markdown()` with markdown special chars (`*`, `_`, `[`, `]`), assert stripped
2. `test_build_document_content_includes_severity` — Verify severity appears in output
3. `test_write_failure_documents_ingestion_error_non_blocking` — Mock `ingest_text` to raise, assert no exception propagated (failure-safe)
4. `test_fix_hints_cover_all_builtin_profiles` — Verify `_FIX_HINTS` has entries for all 8 profile names in `PROFILES`

### Step 5: Resilience check edge cases — add to `test_resilience_check.py` (~3 tests)

1. `test_severity_error_when_very_low_score` — Use HTML that scores very poorly (e.g., all `<style>` blocks, no inline styles), threshold=1.0, verify `severity == "error"` when score < 0.4
2. `test_check_uses_default_profiles` — Verify the check runs default chaos profiles (not zero)
3. `test_check_returns_profile_count_in_details` — Verify details string mentions profile count

### Step 6: Generator edge cases — add to `test_generators.py` (~4 tests)

1. `test_random_email_config_seed_reproducibility` — Same seed → same config fields
2. `test_random_email_config_different_seeds_differ` — Different seeds → different configs
3. `test_build_email_no_dark_mode` — Config with `has_dark_mode=False`, assert no `prefers-color-scheme`
4. `test_build_email_deep_table_nesting` — Config with `table_nesting_depth=8`, assert nested tables generated

### Step 7: Invariant edge cases — add to `test_invariants.py` (~4 tests)

1. `test_contrast_ratio_no_inline_styles_passes` — HTML with no inline color/background, should pass
2. `test_viewport_fit_no_width_attributes_passes` — HTML with no explicit widths, should pass
3. `test_alt_text_empty_alt_fails` — `<img alt="">` should fail (empty alt is not alt presence for non-tracking images)
4. `test_mso_balance_multiple_pairs` — Multiple balanced MSO pairs, should pass

### Step 8: Runner + route edge cases — add to `test_runner.py` (~3 tests)

Runner:
1. `test_run_zero_cases` — `num_cases=0` should return 0 total, 0 passed, 0 failed

Route:
2. `test_property_test_unknown_invariant_returns_422` — Post with `invariants: ["bogus"]`, mock service to raise `DomainValidationError`, assert 422
3. `test_chaos_route_service_error_returns_500` — Mock `run_chaos_test` to raise unexpected error, assert 500

### Step 9: ADR-007 in `docs/ARCHITECTURE.md`

Append after ADR-006:

```markdown
## ADR-007: Rendering Resilience Testing

**Decision:** Three-layer resilience testing — chaos profiles, property-based invariants, and an optional QA check — validates email HTML against real-world client degradation without requiring live rendering infrastructure.

**Context:** Email clients apply destructive transformations to HTML (Gmail strips `<style>` blocks, Outlook removes flexbox, images get blocked). Traditional QA checks validate HTML structure but cannot predict how degraded HTML performs across clients. Property-based testing can find edge cases that hand-written tests miss, but needs domain-specific generators and invariants.

**Solution:** Three complementary testing layers in `app/qa_engine/`:

1. **Chaos Engine** (`chaos/`): 8 pre-built `ChaosProfile` degradation profiles simulate real client behavior (Gmail style strip, Outlook Word engine, image blocking, dark mode inversion, Gmail clipping, mobile narrow viewport, class stripping, media query removal). Each profile is a pure function pipeline — `apply(html) → degraded_html`. Profiles are composable via `compose_profiles()` for compound scenarios. The engine runs all 11 QA checks on both original and degraded HTML, computing a resilience score as `avg(degraded_scores) / original_score`. Critical failures (severity=error) are collected per profile.

2. **Property-Based Testing** (`property_testing/`): 10 `EmailInvariant` checkers (size limit, image width, link integrity, alt text, table nesting, encoding, MSO balance, dark mode readiness, contrast ratio, viewport fit) validate universal email properties. `EmailConfig` frozen dataclass + `build_email()` generator produce synthetic HTML with configurable features. Deterministic seeded RNG enables CI reproducibility; Hypothesis strategies enable interactive exploration. `PropertyTestRunner` orchestrates N test cases with failure collection.

3. **Resilience Check** (`checks/rendering_resilience.py`): Optional QA check #12 that wraps the chaos engine. NOT in `ALL_CHECKS` to prevent recursion — runs separately after the 11 core checks. Pass/fail based on configurable threshold (default 0.7). Severity escalation: <0.4 → error, 0.4–threshold → warning, ≥threshold → info.

**Knowledge feedback loop** (`chaos/knowledge_writer.py`): Critical chaos failures auto-generate knowledge base documents (when enabled + project context provided). Title-based deduplication prevents duplicates. Per-profile fix hints guide remediation. Non-blocking — write failures never break test responses. BOLA-protected via `verify_project_access()`.

**Consequence:** Teams get actionable resilience scores without deploying to real email clients. The property testing layer catches edge cases (oversized images, unbalanced MSO conditionals, low-contrast text) that targeted checks miss. All three layers are pure-CPU with no LLM or database dependency (except knowledge writer), enabling fast CI integration. Each layer is independently feature-flagged (`qa_chaos.enabled`, `qa_property_testing.enabled`, `qa_chaos.resilience_check_enabled`).
```

## Security Checklist (scoped to this feature's endpoints)

No new endpoints in this phase — tests only. Existing endpoints already verified:
- [x] Auth dependency (`get_current_user`) on chaos-test and property-test routes
- [x] Rate limiting (3/min chaos, 1/min property)
- [x] Input validation via Pydantic schemas
- [x] Error responses use `AppError` hierarchy
- [x] No secrets in logs

## Verification

- [ ] `make test` passes — all new + existing tests green
- [ ] `make check` all green (lint, types, tests, security)
- [ ] No regression in existing ~2731 backend tests
- [ ] New tests total: 35+ across 8 files
- [ ] ADR-007 appended to `docs/ARCHITECTURE.md`
