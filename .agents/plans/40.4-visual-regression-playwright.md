# Plan: 40.4 Visual Regression — Playwright HTML Rendering + Pixel Diff

## Context

HTML text snapshots (40.1/40.2) catch structural regressions but miss visual bugs: wrong column gaps, incorrect bgcolor, mismatched font sizes, button radius, text alignment. This adds Playwright-based screenshot + ODiff pixel comparison against Figma `design.png` exports from 40.3.

**Fidelity metric, not pass/fail gate** — mismatch will be ~40-60% initially. Track percentage over time; it decreases with each converter improvement.

## Research Summary

### Existing Infrastructure (reuse heavily)

| Component | Location | What it does |
|-----------|----------|-------------|
| `run_odiff()` | `app/rendering/visual_diff.py:33` | ODiff binary wrapper, returns `DiffResult(identical, diff_percentage, diff_image, pixel_count)` |
| `compare_images()` | `app/rendering/visual_diff.py:119` | High-level: bytes in → `DiffResult` out (writes temp files internally) |
| `capture_screenshot()` | `app/rendering/local/runner.py:45` | Playwright CLI wrapper: HTML → PNG bytes. Uses `npx playwright screenshot` |
| `RenderingProfile` | `app/rendering/local/profiles.py:8` | Viewport/browser config dataclass |
| `DiffResult` | `app/rendering/visual_diff.py:22` | `identical`, `diff_percentage`, `diff_image`, `pixel_count`, `changed_regions` |
| `VisualDiffError` | `app/rendering/exceptions.py` | ODiff error (dimension mismatch, timeout) |

### Snapshot Test Pattern (follow exactly)

| Pattern | Source |
|---------|--------|
| `_load_manifest()` → filter `status: active` | `app/design_sync/tests/test_snapshot_regression.py:34` |
| `_get_active_case_ids()` for parametrize | `test_snapshot_regression.py:43` |
| `_run_conversion(case_dir)` → `ConversionResult` | `test_snapshot_regression.py:83` |
| `_normalize_html()` | `test_snapshot_regression.py:48` |
| `_DEBUG_DIR = Path(__file__).parents[3] / "data" / "debug"` | `test_snapshot_regression.py:30` |

### Case Data

| Case | Dir | Sections | Target | design.png | Reference HTML |
|------|-----|----------|--------|------------|----------------|
| 5 MAAP | `data/debug/5/` | 9 | 13 | **Not yet** | `email-templates/training_HTML/for_converter_engine/maap-kask.html` |
| 6 Starbucks | `data/debug/6/` | 5 | 9 | **Not yet** | `starbucks-pumpkin-spice.html` |
| 10 Mammut | `data/debug/10/` | 12 | 18 | `mammut-duvet-day.html` |

All cases have `design_image: false` in manifest. Tests skip cases without `design.png`. After running 40.3 extract with `--capture-image`, `design.png` appears and `design_image` can be flipped to `true`.

### Image Assets

- `data/design-assets/5/` — 340 PNG files (individual Figma node exports)
- `data/debug/6/VUlWjZGAEVZr/` — case 6 image assets
- Converter output references images via `image_urls` dict or empty `src=""`

### Playwright

- Already in `pyproject.toml` dev-dependencies
- CLI invocation: `npx playwright screenshot --browser cr --viewport-size 600,900 --full-page file://...`
- Existing `capture_screenshot()` writes HTML to temp file, uses `file://` protocol
- **Problem:** `file://` won't resolve relative image paths from converter output. Need HTTP server.

## Test Landscape

- **Pytest markers registered:** `visual_regression`, `snapshot` (both excluded from `make test`)
- **Makefile targets:** `rendering-regression`, `snapshot-test`, `snapshot-capture`, `rendering-baselines`
- **.gitignore patterns:** `data/debug/*/actual.html`, `app/rendering/tests/visual_regression/baselines/_diffs/`

## Type Check Baseline

| Directory | Pyright | MyPy |
|-----------|---------|------|
| `app/design_sync/tests/` | 29 errors, 185 warnings | 0 errors |
| `app/rendering/tests/` | 2 errors, 11 warnings | 0 errors |

Pre-existing errors only — no new errors allowed.

## Files to Create/Modify

| File | Action | Purpose |
|------|--------|---------|
| `app/design_sync/tests/test_snapshot_visual.py` | **Create** | Visual regression tests (converter + reference vs design.png) |
| `data/debug/manifest.yaml` | Modify | Add `visual_threshold` field per case |
| `Makefile` | Modify | Add `snapshot-visual` target |
| `.gitignore` | Modify | Add visual regression artifacts |

## Implementation Steps

### Step 1: Create `app/design_sync/tests/test_snapshot_visual.py`

```python
"""Visual regression: Playwright screenshot of converter output vs Figma design.png.

Renders converter HTML output to a screenshot, pixel-diffs against the Figma design
screenshot captured by 40.3. Measures visual fidelity — how close the converter output
looks to the original design.

This is a fidelity metric, not a pass/fail gate. The mismatch percentage starts high
(~40-60%) and should decrease with each converter improvement.

Run: make snapshot-visual
"""

from __future__ import annotations

import asyncio
import json
import re
import threading
from functools import partial
from http.server import HTTPServer, SimpleHTTPRequestHandler
from pathlib import Path
from typing import Any

import pytest
import yaml

from app.design_sync.converter_service import DesignConverterService
from app.design_sync.diagnose.report import load_structure_from_json, load_tokens_from_json
from app.rendering.local.profiles import RenderingProfile
from app.rendering.local.runner import capture_screenshot
from app.rendering.visual_diff import DiffResult, VisualDiffError, run_odiff

_DEBUG_DIR = Path(__file__).resolve().parents[3] / "data" / "debug"
_MANIFEST = _DEBUG_DIR / "manifest.yaml"
_DESIGN_ASSETS = Path(__file__).resolve().parents[3] / "data" / "design-assets"
_REFERENCE_DIR = (
    Path(__file__).resolve().parents[3]
    / "email-templates"
    / "training_HTML"
    / "for_converter_engine"
)

# Mapping: case ID → reference HTML filename
_REFERENCE_MAP: dict[str, str] = {
    "5": "maap-kask.html",
    "6": "starbucks-pumpkin-spice.html",
    "10": "mammut-duvet-day.html",
}

# Simple profile for visual fidelity screenshots — 600px wide email viewport
_FIDELITY_PROFILE = RenderingProfile(
    name="fidelity_600",
    viewport_width=600,
    viewport_height=900,
    browser="cr",
)
```

### Step 2: HTTP Server Helper

```python
def _serve_directory(root: Path) -> tuple[HTTPServer, int]:
    """Start a localhost HTTP server serving files from root. Returns (server, port)."""
    handler = partial(SimpleHTTPRequestHandler, directory=str(root))
    server = HTTPServer(("127.0.0.1", 0), handler)
    port = server.server_address[1]
    thread = threading.Thread(target=server.serve_forever, daemon=True)
    thread.start()
    return server, port
```

### Step 3: Image Path Rewriting

The converter output may reference images via relative paths (`../../design-assets/5/2833_1135.png`) or empty `src=""`. Rewrite to point at the HTTP server so Playwright can fetch them.

```python
_IMG_SRC_RE = re.compile(r'src="([^"]*)"')

def _rewrite_image_paths(html: str, base_url: str, case_id: str) -> str:
    """Rewrite image src paths to use the HTTP server URL."""
    def _replace(m: re.Match[str]) -> str:
        src = m.group(1)
        if not src or src.startswith("http"):
            return m.group(0)
        # Normalize the path — could be relative or just a filename
        filename = Path(src).name
        return f'src="{base_url}/design-assets/{case_id}/{filename}"'
    return _IMG_SRC_RE.sub(_replace, html)
```

### Step 4: Manifest Helpers

```python
def _load_manifest() -> list[dict[str, Any]]:
    if not _MANIFEST.exists():
        return []
    data = yaml.safe_load(_MANIFEST.read_text())
    return data.get("cases", []) if data else []

def _get_visual_case_ids() -> list[str]:
    """Return case IDs that have design_image: true."""
    return [
        c["id"]
        for c in _load_manifest()
        if c.get("status") == "active" and c.get("design_image") is True
    ]

def _get_visual_threshold(case_id: str) -> float:
    """Get per-case visual threshold (default 0.95 = 95% diff allowed)."""
    for c in _load_manifest():
        if c["id"] == case_id:
            return float(c.get("visual_threshold", 0.95))
    return 0.95
```

### Step 5: Core Test — Run Conversion → Screenshot → Diff

```python
def _run_conversion(case_dir: Path) -> str:
    """Convert design inputs → HTML string."""
    structure = load_structure_from_json(case_dir / "structure.json")
    tokens = load_tokens_from_json(case_dir / "tokens.json")
    result = DesignConverterService().convert(structure, tokens)
    return result.html

async def _screenshot_html(html: str, output_dir: Path, serve_root: Path, case_id: str) -> Path:
    """Render HTML to PNG via Playwright. Returns path to rendered.png."""
    server, port = _serve_directory(serve_root)
    try:
        base_url = f"http://127.0.0.1:{port}"
        rewritten = _rewrite_image_paths(html, base_url, case_id)

        # Write rewritten HTML to a temp file in serve_root so Playwright can fetch it
        html_path = output_dir / "_visual_test.html"
        html_path.write_text(rewritten, encoding="utf-8")

        # capture_screenshot writes to output_dir/{profile.name}.png and returns bytes
        await capture_screenshot(rewritten, _FIDELITY_PROFILE, output_dir)

        # Rename profile-named file to rendered.png
        profile_png = output_dir / f"{_FIDELITY_PROFILE.name}.png"
        rendered_path = output_dir / "rendered.png"
        profile_png.rename(rendered_path)
        return rendered_path
    finally:
        server.shutdown()

async def _diff_images(
    design_path: Path,
    rendered_path: Path,
    output_dir: Path,
    diff_name: str = "diff.png",
) -> DiffResult:
    """Pixel diff two images. Handles dimension mismatch gracefully."""
    diff_path = output_dir / diff_name
    try:
        return await run_odiff(design_path, rendered_path, diff_path, threshold=0.01)
    except VisualDiffError:
        # Dimension mismatch — resize rendered to match design, retry
        # ODiff requires same dimensions; use Pillow to resize
        from PIL import Image

        design_img = Image.open(design_path)
        rendered_img = Image.open(rendered_path)
        if design_img.size != rendered_img.size:
            resized = rendered_img.resize(design_img.size, Image.LANCZOS)
            resized_path = output_dir / "rendered_resized.png"
            resized.save(resized_path)
            return await run_odiff(design_path, resized_path, diff_path, threshold=0.01)
        raise

def _save_report(
    output_dir: Path,
    diff_result: DiffResult,
    threshold: float,
    label: str = "converter",
) -> None:
    """Save visual_report.json with fidelity metrics."""
    report = {
        "label": label,
        "mismatch_pct": diff_result.diff_percentage,
        "pixel_count": diff_result.pixel_count,
        "identical": diff_result.identical,
        "threshold": threshold,
        "passed": diff_result.diff_percentage <= threshold * 100,
    }
    report_path = output_dir / "visual_report.json"
    # Merge with existing report if present
    existing: dict[str, Any] = {}
    if report_path.exists():
        existing = json.loads(report_path.read_text())
    existing[label] = report
    report_path.write_text(json.dumps(existing, indent=2))
```

### Step 6: Test Classes

```python
@pytest.mark.visual_regression
class TestSnapshotVisualRegression:
    """Converter output → screenshot → pixel diff vs Figma design.png."""

    @pytest.mark.parametrize("case_id", _get_visual_case_ids())
    def test_visual_fidelity(self, case_id: str, tmp_path: Path) -> None:
        case_dir = _DEBUG_DIR / case_id
        design_path = case_dir / "design.png"
        assert design_path.exists(), f"No design.png for case {case_id}"

        # 1. Run converter
        html = _run_conversion(case_dir)
        assert html, f"Empty HTML for case {case_id}"

        # 2. Screenshot via Playwright
        serve_root = _DEBUG_DIR.parent  # data/ — so design-assets/ is accessible
        rendered_path = asyncio.run(
            _screenshot_html(html, case_dir, serve_root, case_id)
        )

        # 3. Pixel diff
        threshold = _get_visual_threshold(case_id)
        diff_result = asyncio.run(
            _diff_images(design_path, rendered_path, case_dir)
        )

        # 4. Save artifacts
        _save_report(case_dir, diff_result, threshold, label="converter")

        # 5. Log but don't fail unless above threshold
        pct = diff_result.diff_percentage
        print(f"\n  Case {case_id}: visual mismatch = {pct:.1f}% (threshold {threshold*100:.0f}%)")

        if pct > threshold * 100:
            pytest.fail(
                f"Visual mismatch {pct:.1f}% exceeds threshold {threshold*100:.0f}% "
                f"for case {case_id}. Diff saved to {case_dir / 'diff.png'}"
            )


@pytest.mark.visual_regression
class TestReferenceVisualFidelity:
    """Hand-built reference HTML → screenshot → pixel diff vs design.png.

    Establishes the best achievable fidelity baseline. The gap between
    reference fidelity and converter fidelity is the work remaining.
    """

    @pytest.mark.parametrize("case_id", _get_visual_case_ids())
    def test_reference_fidelity(self, case_id: str, tmp_path: Path) -> None:
        case_dir = _DEBUG_DIR / case_id
        design_path = case_dir / "design.png"
        assert design_path.exists(), f"No design.png for case {case_id}"

        ref_filename = _REFERENCE_MAP.get(case_id)
        if not ref_filename:
            pytest.skip(f"No reference HTML mapping for case {case_id}")
        ref_path = _REFERENCE_DIR / ref_filename
        if not ref_path.exists():
            pytest.skip(f"Reference HTML not found: {ref_path}")

        ref_html = ref_path.read_text(encoding="utf-8")
        serve_root = _DEBUG_DIR.parent

        rendered_path = asyncio.run(
            _screenshot_html(ref_html, case_dir, serve_root, case_id)
        )

        # Rename to reference_rendered.png to avoid clobbering converter rendered.png
        ref_rendered = case_dir / "reference_rendered.png"
        rendered_path.rename(ref_rendered)

        diff_result = asyncio.run(
            _diff_images(design_path, ref_rendered, case_dir, diff_name="reference_diff.png")
        )

        _save_report(case_dir, diff_result, threshold=0.95, label="reference")

        pct = diff_result.diff_percentage
        print(f"\n  Case {case_id} reference: visual mismatch = {pct:.1f}%")
```

### Step 7: Update `data/debug/manifest.yaml`

Add `visual_threshold: 0.95` to each case:

```yaml
cases:
  - id: "5"
    ...
    design_image: false
    visual_threshold: 0.95

  - id: "6"
    ...
    design_image: false
    visual_threshold: 0.95

  - id: "10"
    ...
    design_image: false
    visual_threshold: 0.95
```

### Step 8: Verify `Makefile`

`snapshot-visual` target **already exists** (line 101):
```makefile
snapshot-visual: ## Visual fidelity metrics for snapshot cases (requires Playwright)
	uv run pytest app/design_sync/tests/ -v -m visual_regression --tb=long
```
This runs all `visual_regression`-marked tests in `app/design_sync/tests/`, which includes the new file. **No Makefile change needed.**

### Step 9: Update `.gitignore`

Add:

```gitignore
# Visual regression artifacts (generated by make snapshot-visual)
data/debug/*/rendered.png
data/debug/*/rendered_resized.png
data/debug/*/reference_rendered.png
data/debug/*/diff.png
data/debug/*/reference_diff.png
data/debug/*/visual_report.json
data/debug/*/_visual_test.html
```

### Step 10: Pillow dependency — ALREADY PRESENT

`pillow>=12.1.1` already in `pyproject.toml` (line 28). **No change needed.**

## Implementation Notes

**Why HTTP server instead of `file://`:** Converter output HTML may reference images via relative paths that won't resolve from a temp file location. The HTTP server serves `data/` as root, making `design-assets/{case_id}/` accessible.

**Why `run_odiff` not `compare_images`:** `run_odiff` gives us control over file paths (we need to save diff.png to the case dir, not a temp dir). `compare_images` wraps `run_odiff` in temp files — unnecessary here.

**Dimension mismatch handling:** Figma exports at scale=2 → 1200px wide, Playwright viewport is 600px. ODiff exit code 2 on dimension mismatch. Catch `VisualDiffError`, resize with Pillow, retry.

**asyncio in sync tests:** Use `asyncio.run()` since `capture_screenshot` and `run_odiff` are async but snapshot tests are sync. `asyncio.run()` is deprecated in Python 3.12+; no existing code uses it.

**Cases start with `design_image: false`:** All 3 cases currently lack `design.png`. Tests skip these automatically (`_get_visual_case_ids()` filters on `design_image: true`). After running `python -m app.design_sync.diagnose.extract --connection-id <N> --capture-image`, flip `design_image: true` in manifest.

## Preflight Warnings

- `capture_screenshot` uses `settings.rendering.screenshot_npx_path` — ensure `npx` is on PATH in test env
- ODiff binary via `npx odiff` — ensure `odiff-bin` is in `package.json` devDependencies
- Pillow import is lazy (only on dimension mismatch) — add to `[dev]` extras if missing
- Pre-existing pyright errors (29 in design_sync/tests) — don't let new file add to count

## Security Checklist

- No new endpoints — test-only code
- HTTP server binds `127.0.0.1` only (no external access)
- Server runs on random port, stopped in `finally` block
- No user input — all paths from manifest/filesystem
- No secrets — reads local PNG files only

## Verification

- [ ] `make snapshot-visual` runs without error (all cases skipped if no `design.png`)
- [ ] After adding `design.png` to a case + flipping `design_image: true`: produces `rendered.png` + `diff.png` + `visual_report.json`
- [ ] Reference fidelity test produces `reference_rendered.png` + `reference_diff.png`
- [ ] `visual_report.json` contains both `converter` and `reference` entries
- [ ] Intentionally break converter → mismatch percentage increases
- [ ] `make check` still passes (visual_regression marker excluded from `make test`)
- [ ] Pyright errors ≤ 29 in `app/design_sync/tests/`, ≤ 2 in `app/rendering/tests/`
