# Plan: 53.5 Component Selection — Golden-Reference Confidence Boost — STUB

**Status:** Deferred. Detailed plan promotes when 53.5 starts.

## Summary

`+0.05` confidence boost for components appearing in `email-templates/golden-references/` YAMLs. Doc Part 7 Step 4 §3: *"the engine has no concept of 'renderer track record' (golden references it knows handle this case correctly). Adding a +0.05 confidence boost for components that appear in golden-reference YAMLs would likely improve component selection on edge cases."*

## Approximate Files Affected

- `app/design_sync/component_matcher.py:_score_candidates` — read golden-reference component list, apply boost
- `email-templates/golden-references/index.yaml` — already indexes 14 golden references; iterate components
- New: `app/design_sync/golden_reference_loader.py` — `@lru_cache` golden component set
- New: `app/design_sync/tests/test_golden_boost.py`

## Dependencies

- **Depends on:** Nothing (additive scoring)
- **Blocks:** Nothing downstream

## Open Questions

- Boost magnitude: 0.05 is doc's suggestion. Tune from regression tests.
- Stale boost when components are removed from golden references: cache invalidation by file mtime.

## See

Master: `.agents/plans/50-converter-fidelity-master.md` § Phase 53
Source: `docs/architecture/opus-figma-to-html-process.md` Part 7 Step 4 §3
