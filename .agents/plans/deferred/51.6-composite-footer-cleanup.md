# Plan: 51.6 Composite Footer Cleanup (Gap 4 cleanup) — STUB

**Status:** Deferred. Detailed plan promotes when 51.6 starts. Per advisor flag #1 — avoids re-implementing footer twice.

## Summary

Rename `_fills_footer` → `_fills_footer_legal` (scoped to leaf links + legal section only). Card structure delegated to Rule 1 (51.2) + composite slot (51.1). The composite footer pattern (header logo + name+email text + barcode + bottom shape inside a rounded #ffffff FRAME) becomes a Rule 1 application — no bespoke footer logic.

## Approximate Files Affected

- `app/design_sync/component_matcher.py:1087` — rename `_fills_footer` → `_fills_footer_legal`, narrow scope to links/legal only
- `app/design_sync/component_matcher.py:_match_by_type` — dispatch FOOTER section to either `_fills_footer_legal` (if links/legal leaf) or Rule 1 path (if composite card)
- `email-templates/components/footer-centered.html`, `footer.html` — keep for legal-only path (no changes)
- `email-templates/components/footer-card.html` (NEW?) — or rely on Rule 1 + composite slot rendering
- New: `app/design_sync/tests/test_composite_footer.py`

## Dependencies

- **Depends on:** 51.1 (composite slot), 51.2 (Rule 1 pre-pass), 50.3 (wrapper unwrap — separates user-info row from card from links/legal)
- **Blocks:** Nothing downstream

## Open Questions

- Does composite footer need its own `footer-card.html` template, or does Rule 1's generic rounded-table wrapper suffice? Inspect after 51.2 lands.
- User-info row above the card and links + legal row below stay as their own sections — confirm 50.3 wrapper unwrap separates them correctly.

## See

Master: `.agents/plans/50-converter-fidelity-master.md` § Phase 51
Source: `docs/architecture/opus-figma-to-html-process.md` Gap 4, §8.3 Rules 1+4+5+9 identity exception+11
