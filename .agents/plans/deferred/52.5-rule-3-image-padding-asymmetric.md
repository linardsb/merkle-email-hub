# Plan: 52.5 Rule 3 — Image Padding inside Non-Bleed Container + Asymmetric Padding — STUB

**Status:** Deferred. Detailed plan promotes when 52.5 starts. Per advisor flag, padding asymmetry moved here from 53.

## Summary

Image's `absoluteBoundingBox` strictly inside parent FRAME's bbox + parent has non-default fill → emit `padding: <top> <right> <bottom> <left>` on wrapping `<td>` measured from design (4-tuple per side, NOT single value). Pair with `class="img-pad"` for mobile override (`padding: 0 !important` if stacked image should go full-bleed on mobile — per-design call).

Subsumes Phase 53's "padding asymmetry" item — Rules 1, 3, 6 actions all need 4-tuple padding.

## Approximate Files Affected

- `app/design_sync/figma/layout_analyzer.py` — already has `padding_top/right/bottom/left` on `EmailSection`; verify per-image padding extraction
- `app/design_sync/figma/layout_analyzer.py:ImagePlaceholder` — add `padding_top/right/bottom/left: float | None`
- `app/design_sync/component_matcher.py:_build_token_overrides` — emit asymmetric padding (4-tuple already supported when all 4 set)
- `app/design_sync/correction_applicator.py` — add `class="img-pad"` via 52.3's `add_class`
- `email-templates/shells/*.html` — verify `.img-pad` mobile rule
- New: `app/design_sync/tests/test_rule_3.py`

## Dependencies

- **Depends on:** 52.3 (`add_class` for `img-pad`)
- **Blocks:** Nothing downstream

## Open Questions

- Per-image padding vs section-level padding: today only section-level is extracted. Per-image needs FRAME-walk during `_extract_images`.
- Mobile padding override: always `padding: 0 !important` on stacked, or preserve design's mobile reference if available?

## See

Master: `.agents/plans/50-converter-fidelity-master.md` § Phase 52, advisor flag (asymmetric padding)
Source: `docs/architecture/opus-figma-to-html-process.md` §8.3 Rule 3, Part 7 Step 6
