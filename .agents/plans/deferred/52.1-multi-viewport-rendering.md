# Plan: 52.1 Multi-Viewport Rendering (Part 8 §1 stage 1) — STUB

**Status:** Deferred. Detailed plan promotes when 52.1 starts.

## Summary

`run_verification_loop()` extended with `viewports: tuple[int, ...] = (600, 480)`. Renders HTML at both, compares both before applying corrections. Doc Part 8 §1: *"single-viewport rendering is the most common cause of 'fixed in one place, broken in another' loops."*

## Approximate Files Affected

- `app/design_sync/visual_verify.py:181` (`run_verification_loop`) — viewport loop
- `app/design_sync/visual_verify.py:92` (`compare_sections`) — accept `viewport: int`
- `app/rendering/` — Playwright multi-viewport renderer
- `app/core/config.py:DesignSyncConfig` — `verify_viewports: tuple[int, ...] = (600, 480)`
- New: `app/design_sync/tests/test_multi_viewport.py`

## Dependencies

- **Depends on:** Phase 50 complete (otherwise wasted effort if section structure changes)
- **Blocks:** 52.4, 52.5, 52.6 (rules that fire differently per viewport)

## Open Questions

- Master Out-of-band Decision #4: do we add mobile snapshot baselines to all 4 existing regression cases (cost: regenerate baselines) or run mobile only as a secondary check?
- Performance: doubles Playwright time. Mitigation in master Risks: skip mobile when desktop fidelity ≥ 0.85.

## See

Master: `.agents/plans/50-converter-fidelity-master.md` § Phase 52
Source: `docs/architecture/opus-figma-to-html-process.md` Part 8 §1
