# Plan: 52.6 Rule 6 — Mobile DOM Order via `dir="rtl"` Flip Pattern — STUB

**Status:** Deferred. Detailed plan promotes when 52.6 starts.

## Summary

Image-right desktop layout where mobile expects image-on-top → DOM order `[image][content]` + `direction: rtl` on parent at desktop only (mobile media query restores `ltr`). Per-side padding flipped to match. MSO conditional path `<table dir="rtl">` for Outlook.

**Verified working markup**: `email-templates/training_HTML/for_converter_engine/Lego/manual_component_build.html` sections #10 and #12 — pattern can be copied directly.

**Gotcha** (doc Rule 6): `direction: rtl` MUST live on a class with a mobile override, NOT inline — when columns become `display: block` on mobile, rtl bleeds into block-layout context and silently kills text column's left padding.

## Approximate Files Affected

- `app/design_sync/component_renderer.py` — emit `class="flip-row"` + `dir="rtl"` on parent + `dir="ltr"` on each child div
- `email-templates/components/zigzag-image-right.html` — `dir="rtl"` flip pattern markup (verified working)
- `email-templates/shells/*.html` — `.flip-row` CSS + mobile override (`@media max-width: 599px { .flip-row { direction: ltr !important } }`)
- `app/design_sync/component_matcher.py` — detect image-right zigzag in column_groups, emit dir="rtl" flag
- `app/design_sync/correction_applicator.py` — `52.3` `wrap_in` for MSO conditional path
- New: `app/design_sync/tests/test_rule_6.py`

## Dependencies

- **Depends on:** 52.1 (multi-viewport — needed to detect mobile mismatch), 52.3 (structural mutator), 52.5 (per-side padding for the flipped layout)
- **Blocks:** Nothing downstream

## Open Questions

- Always emit `dir="rtl"` on image-right zigzag, or only when mobile reference frame disagrees with DOM order? Doc says always — it's a deterministic improvement.
- Compatibility: verified across Apple Mail, Gmail, Outlook 2007+, AOL, Yahoo. Document any gaps.

## See

Master: `.agents/plans/50-converter-fidelity-master.md` § Phase 52
Source: `docs/architecture/opus-figma-to-html-process.md` §8.3 Rule 6 (verified markup excerpts)
