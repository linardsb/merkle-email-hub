# Plan: 51.2 Rule 1 — Card with N Children Collapses to One Container — STUB

**Status:** Deferred. Detailed plan promotes when 51.2 starts. Hard dep on 51.1.

## Summary

Rule 1 pre-pass: a `FRAME` with `fills[0]` non-default + `cornerRadius > 0` + ≥2 child frames → wraps children in one rounded outer table. Tags children with `parent_card_id`. Renderer groups consecutive sections sharing a `parent_card_id`. Reuses primitive `sibling_detector.py` already produces (Phase 49.1 / 49.2). Emits a composite slot containing the heterogeneous children.

Canonical case: LEGO footer FRAME `2833:2057` (4 heterogeneous children inside one #ffffff rounded frame).

## Approximate Files Affected

- `app/design_sync/frame_rules.py` (50.5) — add `rule_1_card_with_n_children` predicate
- `app/design_sync/figma/layout_analyzer.py` — pre-pass tagging children with `parent_card_id`
- `app/design_sync/figma/layout_analyzer.py` — `EmailSection.parent_card_id: str | None`
- `app/design_sync/component_renderer.py` — group consecutive sections sharing `parent_card_id`, wrap in rounded outer table
- `app/design_sync/component_matcher.py` — emit composite slot when Rule 1 fires
- New: `app/design_sync/tests/test_rule_1.py`

## Dependencies

- **Depends on:** 50.5 (`frame_rules.py` module exists), 51.1 (composite slot infrastructure)
- **Blocks:** 51.6 (composite footer rewrite — Gap 4 collapses into Rule 1 application)

## Open Questions

- Where does Rule 1's pre-pass live? `layout_analyzer.py` (during section construction) vs `frame_rules.py` (post-pass tagger). Master Out-of-band Decision #2 favors `frame_rules.py`.
- Identity-card exception (50.7): does Rule 1 still fire on physical-card surfaces? Yes — it just wraps; Rule 9's auto-flip is separate (52.7 reads `is_physical_card_surface`).

## See

Master: `.agents/plans/50-converter-fidelity-master.md` § Phase 51, advisor flag #1
Source: `docs/architecture/opus-figma-to-html-process.md` §8.3 Rule 1, Gap 4
