# Plan: 51.4 Spec Mini-Table Slot (Gap 3) — STUB

**Status:** Deferred. Detailed plan promotes when 51.4 starts.

## Summary

New `_score_extended_candidates` trigger: ≥2 images ≤30px wide, each sibling-paired with a text < 40 chars. Confidence 0.92 with 2+ pairs. Spec-list emitted as composite slot containing N `(icon_url, label)` tuples — one cell pair per item. Used as a *child* component nested inside zigzag rows.

Canonical case: LEGO product cards' 4-cell mini table `[icon] 617 Pieces  [icon] +260 LEGO Insiders Points` (sections 10–13).

## Approximate Files Affected

- `app/design_sync/component_matcher.py:_score_extended_candidates` — new spec-list trigger
- `app/design_sync/component_matcher.py` — `_fills_spec_list` filler
- `email-templates/components/spec-list.html` — new component (single-row table, one cell pair per `(icon, label)` tuple)
- Alternatively: extend `zigzag-image-{left,right}.html` with `<tr data-slot-repeat="specs">` row (cheaper but less clean)
- New: `app/design_sync/tests/test_spec_list.py`

## Dependencies

- **Depends on:** 51.1 (composite slot — list of `(icon_url, label)` tuples is the canonical use case)
- **Blocks:** Nothing downstream

## Open Questions

- Standalone component (`spec-list.html`) vs sub-row of existing components? Master notes the latter is cheaper. Decide based on prevalence in real designs.
- Should the slot type be `composite[list[tuple]]` or a custom `SpecListSlot`? Resolve at 51.1 promotion.

## See

Master: `.agents/plans/50-converter-fidelity-master.md` § Phase 51
Source: `docs/architecture/opus-figma-to-html-process.md` Gap 3
