# Plan: 53.2 Hero-Text-Only + Hero-Image Pairing (Gap 6) — STUB

**Status:** Deferred. Detailed plan promotes when 53.2 starts.

## Summary

Pair-detection: section A has 600px-wide image bottom-aligned; section B has texts + ≤1 button on non-white bg; A.bottom_color ≈ B.top_color (50.2 boundary classifier supplies). Merge into single `hero-block` component or tag both with shared `hero_pair_id`. The renderer emits a single `hero-block`/`hero-text` component with the image as background and the text + CTA as overlay.

Today: text-only hero halves are routed into `CONTENT` (`_match_by_type` requires `has_images` for HERO).

## Approximate Files Affected

- `app/design_sync/figma/layout_analyzer.py` — new pair-merger pass between `detect_repeating_groups()` and `match_all()`
- `app/design_sync/figma/layout_analyzer.py:_classify_by_content` — mark text-on-coloured-bg sections as `hero-text-candidate`
- `app/design_sync/component_matcher.py:_match_by_type` — accept hero-text pair, route to combined hero component
- `email-templates/components/hero-block.html` — verify supports image+text+CTA composite
- New: `app/design_sync/tests/test_hero_pair.py`

## Dependencies

- **Depends on:** 50.2 (boundary classifier supplies bottom/top color match)
- **Blocks:** Nothing downstream

## Open Questions

- Should the pair-merger emit a single `EmailSection` or two with `hero_pair_id`? Single section is cleaner; two preserves design tree fidelity.

## See

Master: `.agents/plans/50-converter-fidelity-master.md` § Phase 53
Source: `docs/architecture/opus-figma-to-html-process.md` Gap 6
