# Plan: 52.2 Categorized Correction Schema (Part 8 §1 stage 3) — STUB

**Status:** Deferred. Detailed plan promotes when 52.2 starts.

## Summary

VLM output schema constrained to `rule_id: Literal["rule_1", ..., "rule_11", "content_error"]`. `CorrectionApplicator` dispatches per rule. Rule mismatches surface as low-confidence telemetry rather than ad-hoc fixes.

## Approximate Files Affected

- `app/design_sync/visual_verify.py` — VLM correction extraction prompt + schema constraint
- `app/design_sync/correction_applicator.py` — per-rule dispatch
- New: `app/design_sync/correction_schemas.py` — `CorrectionRule` Literal + `CategorizedCorrection` dataclass
- New: `app/design_sync/tests/test_correction_schema.py`

## Dependencies

- **Depends on:** 50.5 (rule predicates exist for dispatch reference), 52.1 (viewport in correction context)
- **Blocks:** 52.3 (structural applicator dispatches by `rule_id`), 52.10 (telemetry logs `rule_id`)

## Open Questions

- Should the VLM be allowed to emit "content_error" for divergences that aren't structural (wrong copy, wrong asset)? Doc §8.1 stage 3 suggests yes.
- Confidence threshold for accepting a categorized correction (default 0.7?).

## See

Master: `.agents/plans/50-converter-fidelity-master.md` § Phase 52
Source: `docs/architecture/opus-figma-to-html-process.md` Part 8 §1 stage 3
