# Plan: 53.3 Per-Frame Export as Default (Gap 7) — STUB

**Status:** Deferred. Detailed plan promotes when 53.3 starts.

## Summary

`image_resolution_mode: "imageRef" | "frame_export"` toggle on `DesignSyncConfig`. Default `frame_export` for any frame whose node has children other than the imageRef-bearing rectangle (composited); `imageRef` for leaf rectangle. One extra batched `/v1/images` call per email but it's batched (single call for all frames).

Per-frame `/v1/images?ids=…` rendering at `scale=2` returns the design-as-rendered, which is what every email-design visual-verification step actually wants to compare against. raw `imageRef`-based S3 URLs miss masks/transforms/composited vector decoration.

## Approximate Files Affected

- `app/design_sync/figma/service.py` — image-extraction path; toggle logic per FRAME structure
- `app/core/config.py:DesignSyncConfig` — `image_resolution_mode` field
- `app/design_sync/figma/service.py:export_frame_screenshots` — already exists for batched per-frame export; reuse
- New: `app/design_sync/tests/test_image_resolution_mode.py`

## Dependencies

- **Depends on:** Nothing
- **Blocks:** Nothing downstream

## Open Questions

- Cost: one extra batched API call per email — acceptable per doc. Confirm with rate-limit budget.
- Detection rule for "composited": "FRAME with children other than imageRef-bearing rectangle" — current implementation may need refinement.

## See

Master: `.agents/plans/50-converter-fidelity-master.md` § Phase 53
Source: `docs/architecture/opus-figma-to-html-process.md` Gap 7, Part 4 §4
