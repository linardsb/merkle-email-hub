# Plan: 52.8 Rule 4 — Visible Divider Absent from FRAME Tree (needs PNG) — STUB

**Status:** Deferred. Detailed plan promotes when 52.8 starts.

## Summary

PNG diff: ≥2 sibling columns visibly separated by uniform 1-2px line, no LINE/RECTANGLE/TEXT node containing `|`/`·`/`–` between them in FRAME tree → emit `<td>` containing literal `|` + `color: #c8c8c8; padding: 0 14px;` (most robust across mail clients).

## Approximate Files Affected

- `app/design_sync/visual_verify.py` — VLM prompt extracts "visible divider absent" from category 4
- `app/design_sync/correction_applicator.py` — `_apply_divider_insertion()` per Rule 4 dispatch
- `app/design_sync/component_renderer.py` — render literal `|` divider `<td>` between columns
- New: `app/design_sync/tests/test_rule_4.py`

## Dependencies

- **Depends on:** 50.1 (full-design PNG required), 52.2 (categorized schema), 52.3 (structural insertion)
- **Blocks:** Nothing downstream

## Open Questions

- Variant choice: literal `|` (most robust) vs 1px `<td>` line vs `<hr>`. Doc says (a) is most robust — start there; revisit if real designs need (b)/(c).
- Detection threshold for "uniform-coloured 1–2px-wide line": image-recognition or VLM-only?

## See

Master: `.agents/plans/50-converter-fidelity-master.md` § Phase 52
Source: `docs/architecture/opus-figma-to-html-process.md` §8.3 Rule 4
