# Plan: 52.10 Rule Telemetry — STUB

**Status:** Deferred. Detailed plan promotes when 52.10 starts.

## Summary

Emit structured event per applied correction:

```python
log.info("conversion.correction_applied",
    rule_id="rule_1_card_with_n_children" | ... | "rule_11_card_width_matches_image",
    section_id=section.node_id,
    viewport=600 | 480 | "dark",
    iteration=loop_iter,
    diff_severity="major" | "minor",
)
```

After 100+ conversions, the histogram of `rule_id` counts tells the team which detection rules pay back the most engineering effort. Doc Part 8 §5: "a typical e-commerce email is likely to fire rules 1, 2, 3, 9, 10, 11 most often; a transactional email is likely to fire rules 4, 6, 7, 8."

## Approximate Files Affected

- `app/design_sync/correction_applicator.py` — emit log event after each successful correction
- `app/design_sync/visual_verify.py:run_verification_loop` — pass `iteration` and `viewport` to applicator
- New: `app/design_sync/tests/test_correction_telemetry.py` — assert event fields

## Dependencies

- **Depends on:** 52.2 (categorized schema with `rule_id`), 52.1 (viewport in correction context)
- **Blocks:** Nothing downstream — but enables data-driven rule prioritization for Phase 53+

## Open Questions

- Aggregation: log to structlog only, or also to a dedicated metrics sink (Loki, Prometheus)?
- Sampling: log every correction, or sample at scale to avoid log volume?

## See

Master: `.agents/plans/50-converter-fidelity-master.md` § Phase 52
Source: `docs/architecture/opus-figma-to-html-process.md` Part 8 §5
