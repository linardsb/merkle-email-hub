# Plan: 38.4 Converter, MJML Generator & Template Engine Fixes

## Context
Fix 16 bugs in `converter.py`, `mjml_generator.py`, `mjml_template_engine.py`, and `section_cache.py`. Enforce golden template quality patterns (G-REF-1 through G-REF-6) in all converter output. The converter is the final HTML generation stage — bugs here produce broken emails in real clients.

## Research Summary

### Target Files

| File | Lines | Role |
|------|-------|------|
| `app/design_sync/converter.py` | 1324 | Design tree → email HTML (tables, text, buttons, MSO) |
| `app/design_sync/mjml_generator.py` | 434 | EmailSection + ExtractedTokens → MJML markup |
| `app/design_sync/mjml_template_engine.py` | 208 | Jinja2 MJML templates with design tokens |
| `app/design_sync/section_cache.py` | 317 | Two-layer cache (LRU + Redis) for section HTML |

### Key Data Models

```
TextBlock(node_id, content, font_size?, is_heading, font_family?, font_weight?, line_height?, letter_spacing?)
  — NOTE: no text_color field currently

EmailSection(section_type, node_id, node_name, width?, height?, column_layout, column_count,
  texts: list[TextBlock], images: list[ImagePlaceholder], buttons: list[ButtonElement],
  bg_color?, padding_top/right/bottom/left?, item_spacing?, column_groups)
```

### Import Patterns
- `from app.design_sync.figma.layout_analyzer import EmailSection, TextBlock, EmailSectionType, ColumnLayout`
- `from app.design_sync.protocol import ExtractedTokens, ExtractedColor`
- `from app.projects.design_system import BrandPalette, Typography`
- `from app.core.logging import get_logger`

## Test Landscape

### Existing Test Files

| File | Tests | Coverage |
|------|-------|----------|
| `test_penpot_converter.py` | 35+ | CSS sanitization (6), colors, typography, font stack, node→HTML, grouping |
| `test_mjml_generator.py` | 45+ | MJML basic, preheader, columns, elements, markers, security, tokens |
| `test_mjml_templates.py` | 45+ | Template resolution, render, escaping, context building, button height |
| `test_section_cache.py` | 25+ | Hash computation, cache entry, memory, async, converter integration |
| `test_converter_fixes.py` | — | Has visible content, empty frame pruning |

### Factories Available

| Factory | File | Creates |
|---------|------|---------|
| `_make_section()` | test_section_cache, test_mjml_generator | EmailSection |
| `_make_tokens()` | test_section_cache, test_mjml_generator | ExtractedTokens |
| `_make_entry()` | test_section_cache | SectionCacheEntry |
| `_palette()` | test_mjml_templates | BrandPalette |
| `_typo()` | test_mjml_templates | Typography |
| `_ctx()` | test_mjml_templates | MjmlTemplateContext |
| `_text()`, `_image()`, `_button()` | test_mjml_templates | Element factories |
| `_NodeProps` | test_penpot_converter | Converter node properties |

### Golden Template Patterns (from `app/ai/templates/library/`)

| Pattern | Example | Source |
|---------|---------|--------|
| G1 `role="presentation"` | `<table role="presentation" cellpadding="0" cellspacing="0">` | All templates |
| G-REF-2 image styles | `display: block; width: 100%; height: auto; border: 0;` | promotional_hero.html:48 |
| G-REF-6 format-detection | `<meta name="format-detection" content="telephone=no, date=no, address=no, email=no, url=no">` | All templates head |
| G-REF-6 mso-hide | `style="display: none; max-height: 0; overflow: hidden; mso-hide: all;"` | Preheader divs |
| G-REF-6 Outlook dark mode | `[data-ogsc] .dark-bg { ... }` + `[data-ogsb] .dark-text { ... }` | promotional_hero.html:32-33 |
| G12 font-size:0 | `font-size: 0; line-height: 0;` on column container `<td>` | Multi-column layouts |

### Component Seeds
- Seed #0 (Email Shell): Complete document wrapper with VML namespaces, MSO settings, dark mode base classes, `[data-ogsc]` selectors, 8-client compatibility map.

## Type Check Baseline

| Tool | Errors | Details |
|------|--------|---------|
| **Pyright** | 0 errors, 3 warnings | 2× `reportPrivateUsage` in mjml_generator, 1× in mjml_template_engine |
| **Mypy** | 5 errors | converter.py: 3 (no-redef L824, assignment L850-851); section_cache.py: 1 (unused-ignore L275) |

Target: ≤ 5 mypy errors, 0 pyright errors after changes.

## Bug Table

| # | Sev | File:Line | Bug | Fix |
|---|-----|-----------|-----|-----|
| 20 | CRIT | converter.py:101-109 | `_sanitize_css_value` regex strips `()` — kills `rgb()`, `hsl()` | Allow balanced parens; block `expression()`, `url(javascript:)` specifically |
| 21 | HIGH | converter.py:701-704, mjml_generator.py:152-158 | `padding_top or 0` treats `0.0` as falsy | `x if x is not None else default` everywhere |
| 22 | HIGH | converter.py:436-487 | Duplicate Outlook button (VML + HTML both render) | Wrap HTML button in `<!--[if !mso]><!--> ... <!--<![endif]-->` |
| 23 | HIGH | mjml_template_engine.py:105-112 | Template context missing per-text props | Thread `text.font_size/weight/family/color` into Jinja2 context |
| 24 | HIGH | mjml_template_engine.py:105-112 | All text uses `typo.base_size` | Per-text style dict in template context |
| 25 | HIGH | section_cache.py:62-87 | Cache hash missing text styling fields | Add `font_size`, `font_weight`, `font_family`, `text_color`, `is_heading` to canonical |
| 26 | MED | converter.py:362-373 | Missing `mso-line-height-rule:exactly` on headings | Add to heading `<h>` tag inline style |
| 27 | MED | converter.py:487 | VML button `stroke="f"` not proper XML | Use `stroke="false"` |
| 28 | MED | mjml_generator.py:125-130 | Dark mode falsy: `if dark:` skips `#000000` | `if dark is not None:` |
| 29 | MED | mjml_generator.py:181 | Sanitized empty string falls through to unsanitized | Check `result != ""` explicitly |
| 30 | MED | mjml_generator.py:61 | `<mj-body>` placement outside `<mj-head>` | Fix template string structure |
| 31 | MED | mjml_template_engine.py:183-184 | Dark mode CSS falsy check drops valid entries | `if name is not None and hex_val is not None:` |
| 32 | MED | mjml_template_engine.py:197 | `<mj-body>` self-closing in template | Use proper open/close tags |
| 33 | LOW | converter.py:636 | Image missing `display:block` | Add to inline style |
| 34 | LOW | converter.py:~1180 | Column width zero-division | Guard `container_width > 0` |
| 35 | LOW | converter.py:1040-1043 | `sanitize_web_tags_for_email` CSS gaps | Block `url()`, `expression()`, unicode escapes |

### Golden Pattern Enforcement

| ID | Pattern | Enforcement Point |
|----|---------|-------------------|
| G1 | `role="presentation"` on all `<table>` | `_render_table_open()` |
| G-REF-2 | `display:block; width:100%; height:auto; border:0;` + HTML `width` on `<img>` | `_render_image()` / `node_to_email_html(IMAGE)` |
| G-REF-6a | `<meta name="format-detection">` in `<head>` | Shell/head generation |
| G-REF-6b | `mso-hide: all` on preheader | Preheader rendering |
| G-REF-6c | `[data-ogsc]`/`[data-ogsb]` alongside `@media` dark mode | Dark mode CSS generation |
| G12 | `font-size: 0` on column container `<td>` | Multi-column `<td>` rendering |

## Implementation Steps

### Step 1: Fix Bug 20 — CSS Sanitizer (CRITICAL)
**File:** `converter.py:101-109`

Replace regex that strips all `()` with targeted blocking:
- Allow balanced parentheses for `rgb()`, `hsl()`, `rgba()`, `hsla()`, `calc()`, `clamp()`
- Block `expression(`, `url(javascript:`, `url(data:text/html`, `-moz-binding`
- Strip `; {} \ < >` and control characters
- Test: `rgb(255, 0, 0)` → preserved. `expression(alert(1))` → stripped. `url(javascript:x)` → stripped.

### Step 2: Fix Bug 21 — Falsy Traps (HIGH)
**Files:** `converter.py:701-704`, `mjml_generator.py:152-158`, `converter.py:436-437`

Search-and-replace pattern across all 4 files:
- `x or default` → `x if x is not None else default` for all numeric/color properties
- Specifically: `padding_top or`, `padding_right or`, `padding_bottom or`, `padding_left or`, `bg_color or`, `fill_color or`, `font_size or`, `item_spacing or`

### Step 3: Fix Bug 22 — Duplicate Outlook Button (HIGH)
**File:** `converter.py:436-487`

Wrap the standard HTML button in MSO conditional:
```html
<!--[if !mso]><!-->
<a href="..." style="...">Button Text</a>
<!--<![endif]-->
```
Keep VML block inside `<!--[if mso]>...<![endif]-->`.

### Step 4: Fix Bugs 23-24 — Per-Text Template Properties (HIGH)
**File:** `mjml_template_engine.py:105-112`

Build per-text style list from `section.texts`:
```python
text_styles = [
    {"font_size": t.font_size, "font_weight": t.font_weight,
     "font_family": t.font_family, "is_heading": t.is_heading}
    for t in section.texts
]
```
Add `text_styles` to Jinja2 context dict. Update templates to use `text_styles[loop.index0]` per text block.

### Step 5: Fix Bug 25 — Section Cache Hash (HIGH)
**File:** `section_cache.py:62-87`

Add to `_section_to_canonical()`:
```python
"texts": [
    {"content": t.content, "font_size": t.font_size, "font_weight": t.font_weight,
     "font_family": t.font_family, "is_heading": t.is_heading}
    for t in section.texts
],
```

### Step 6: Fix Bugs 26-35 (MEDIUM/LOW)
Per bug table above. Each is a targeted 1-5 line fix at the specified location.

### Step 7: Golden Pattern Enforcement
- G1: Add `role="presentation"` to `_render_table_open()` output
- G-REF-2: Ensure `_render_image()` always includes `display:block; width:100%; height:auto; border:0;` inline + HTML `width` attr
- G-REF-6: Add `format-detection` meta to head generation, `mso-hide:all` to preheader, `[data-ogsc]`/`[data-ogsb]` to dark mode CSS
- G12: Add `font-size:0` to multi-column container `<td>` style

### Step 8: Security Validation
- Verify `expression()` blocked by sanitizer
- Verify `url(javascript:)` blocked
- Verify `\0` null byte stripped
- Verify `var(--evil)` blocked
- Add security test cases

### Step 9: Write 28 New Tests
Distribution across test files:

| File | New Tests | Focus |
|------|-----------|-------|
| `test_penpot_converter.py` | 8 | CSS sanitizer (rgb preserved, expression blocked, url(js) blocked, unicode, CSS vars, balanced parens), image display:block, heading mso-line-height |
| `test_mjml_generator.py` | 7 | Dark mode #000000 preserved, padding 0.0 preserved, sanitized empty fallback, mj-body placement, [data-ogsc] selectors, column font-size:0, preheader mso-hide |
| `test_mjml_templates.py` | 6 | Per-text font_size threading, per-text font_weight, per-text font_family, mj-body structure, format-detection meta, role="presentation" audit |
| `test_section_cache.py` | 5 | Hash includes font_size, hash includes font_weight, hash includes font_family, hash includes is_heading, cache invalidates on text style change |
| `test_converter_fixes.py` | 2 | Duplicate button MSO conditional, column zero-width guard |

### Step 10: Run Verification
```bash
make check  # All lint + types + tests
```
Verify pyright ≤ 0 errors, mypy ≤ 5 errors.

## Preflight Warnings
- `_sanitize_css_value` is imported as private by mjml_generator and mjml_template_engine (3 pyright warnings). Consider making it public or adding `# pyright: ignore` if keeping private.
- `converter.py:824` has a mypy `no-redef` on `style_parts` — existing issue, don't regress.
- `converter.py:850-851` has mypy `assignment` errors (int|str → str) — existing, don't regress.
- `section_cache.py:275` has unnecessary `type: ignore` — can clean up.
- TextBlock currently has no `text_color` field. Bug 25 fix should add text styling fields that DO exist (font_size, font_weight, font_family, is_heading). If text_color is needed, it must be added to the TextBlock dataclass in layout_analyzer.py first (upstream change).

## Security Checklist
- [x] CSS sanitizer blocks `expression()`, `url(javascript:)`, `-moz-binding`, unicode escapes
- [x] CSS sanitizer preserves safe functions: `rgb()`, `hsl()`, `rgba()`, `hsla()`
- [x] HTML escaping on all text content (existing `html.escape()` preserved)
- [x] No user input reaches `sa.text()`, `subprocess`, `eval`
- [x] Button `href` sanitized (existing check preserved)
- [x] XSS test cases added for each sanitizer change
- [ ] No new endpoints (N/A — internal pipeline changes only)

## Verification
- [ ] `make check` passes (lint + types + tests)
- [ ] `rgb(255, 0, 0)` survives CSS sanitizer
- [ ] `expression(alert(1))` blocked by CSS sanitizer
- [ ] `padding: 0` → `padding="0"` in MJML (not dropped)
- [ ] Per-text fonts rendered in template output
- [ ] Cache invalidates on `font_size` change
- [ ] Single Outlook button (not duplicated)
- [ ] Every `<table>` has `role="presentation"`
- [ ] Every `<img>` has `display: block` + `width`
- [ ] Dark mode CSS includes `[data-ogsc]`/`[data-ogsb]`
- [ ] Preheader has `mso-hide: all`
- [ ] `format-detection` meta in head
- [ ] Column `<td>` has `font-size: 0`
- [ ] Pyright errors ≤ 0 (baseline: 0)
- [ ] Mypy errors ≤ 5 (baseline: 5)
- [ ] 28 new tests pass
- [ ] Existing 150+ tests still pass
