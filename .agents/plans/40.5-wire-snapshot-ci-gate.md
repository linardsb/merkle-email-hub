# Plan: 40.5 Wire Snapshot Regression Tests into CI Gate

## Context

Promote snapshot regression tests from isolated (`@pytest.mark.snapshot`) to CI-blocking. The `and not snapshot` exclusion in `make test` was temporary while cases were being set up (40.2). All 3 cases are active and green — time to gate merges on them.

## Research Summary

| File | Line(s) | Current State |
|------|---------|---------------|
| `Makefile:54` | `uv run pytest -v -m "not integration and not benchmark and not visual_regression and not collab and not snapshot"` | Excludes snapshot |
| `Makefile:95-99` | `snapshot-test` and `snapshot-capture` targets | Exist |
| `Makefile:101` | `check: lint types test check-fe ...` | Depends on `test` — inherits promotion |
| `pyproject.toml:262` | `"snapshot: Snapshot regression tests against hand-verified expected.html (run with: make snapshot-test)"` | Needs update |
| `CLAUDE.md:38-39` | Lists `snapshot-test` and `snapshot-capture` | Missing `snapshot-visual` note |
| `.github/workflows/ci.yml:41` | `uv run pytest -v -m "not integration"` | Already includes snapshot tests |

**Key insight:** CI workflow already doesn't exclude snapshot — only the local `make test` does.

## Test Landscape

**File:** `app/design_sync/tests/test_snapshot_regression.py` (180 lines)

| Class | Method | Mark | Cases |
|-------|--------|------|-------|
| `TestSnapshotRegression` | `test_snapshot_matches` | `@pytest.mark.snapshot` | 3 (IDs 5, 6, 10) |
| `TestSnapshotSanity` | `test_case_loads` | None (always runs) | 3 |
| `TestSnapshotSectionCount` | `test_section_count` | `@pytest.mark.snapshot` | 3 |

**Total:** 9 parametrized invocations. All pass in ~1s. No network/DB deps.

## Type Check Baseline

Not applicable — this task modifies only `Makefile`, `pyproject.toml`, and `CLAUDE.md`. No Python code changes.

## Files to Modify

1. **`Makefile:54`** — Remove `and not snapshot` from pytest filter
2. **`Makefile`** — Add `snapshot-visual` target (stub pointing to 40.4 visual fidelity scripts)
3. **`pyproject.toml:262`** — Update snapshot marker description
4. **`CLAUDE.md`** — Update Essential Commands section

## Implementation Steps

### Step 1: Makefile — Remove snapshot exclusion

`Makefile:54` — change:
```
-m "not integration and not benchmark and not visual_regression and not collab and not snapshot"
```
to:
```
-m "not integration and not benchmark and not visual_regression and not collab"
```

### Step 2: Makefile — Add `snapshot-visual` target

After `snapshot-capture` target (~line 99), add:
```makefile
snapshot-visual: ## Visual fidelity metrics for snapshot cases (requires Playwright)
	uv run pytest app/design_sync/tests/ -v -m "visual_regression" --tb=long
```

**Note:** If 40.4 hasn't created visual regression tests yet, this target is a placeholder. The key deliverable for 40.5 is the CI gate promotion.

### Step 3: pyproject.toml — Update marker description

`pyproject.toml:262` — change:
```
"snapshot: Snapshot regression tests against hand-verified expected.html (run with: make snapshot-test)",
```
to:
```
"snapshot: Snapshot regression tests — runs in make test/check CI gate (standalone: make snapshot-test)",
```

### Step 4: CLAUDE.md — Update Essential Commands

After `make snapshot-capture` line, add note about `snapshot-visual`. Update `snapshot-test` description to note it's now redundant but kept for convenience:
```
make snapshot-test        # Snapshot regression tests (now included in make test; standalone convenience)
make snapshot-capture CASE=5  # Capture converter output for visual review
make snapshot-visual      # Visual fidelity metrics (requires Playwright, separate from CI)
```

## Preflight Warnings

- **Pre-existing failure:** `test_hero_block_roundtrip` in `test_golden_roundtrip.py` fails independently. Not related to snapshot tests. Confirm it fails before AND after this change.
- **`make snapshot-visual`:** Task spec says "Verify exists (added in 40.4)" but the target does NOT exist yet. Create a reasonable placeholder.

## Verification

- [ ] `make test` runs all 9 snapshot tests (3 regression + 3 sanity + 3 section count)
- [ ] `make check` passes (snapshot tests included)
- [ ] `make snapshot-test` still works standalone
- [ ] Intentionally break `expected.html` → `make test` fails
- [ ] `make snapshot-visual` target exists and runs
- [ ] No pyright/mypy impact (no Python code changes)
