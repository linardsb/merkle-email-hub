# Plan: 47.1 Section-Level Screenshot Cropping Utility

## Context

The visual verification loop (47.x) compares at section granularity, not full-page. Section bounds come from `EmailSection.y_position` and `EmailSection.height` (layout analysis). A `crop_section()` utility extracts individual section images from full-page Playwright screenshots, enabling precise per-section CSS corrections instead of vague full-page diffs.

## Research Summary

| File | Role |
|------|------|
| `app/rendering/local/service.py:39` | `render_screenshots()` → returns `image_bytes: bytes` (full-page PNG) |
| `app/design_sync/figma/layout_analyzer.py:116` | `EmailSection` dataclass — `y_position: float \| None`, `height: float \| None` |
| `app/design_sync/visual_verify.py` | Consumer — `compare_sections()` needs per-section PNGs |
| `app/design_sync/visual_scorer.py:114` | `score_fidelity()` — inline section cropping via numpy slicing (pattern to extract) |
| `app/design_sync/assets.py:46` | Pillow pattern: `BytesIO` ↔ PIL Image, try/except ImportError |
| `app/design_sync/image_sampler.py:16` | `Image.crop((left, top, right, bottom))` usage |
| `app/rendering/exceptions.py` | `ScreenshotRenderError` — custom exception base |

**Pillow pattern (from `assets.py`):** `Image.open(BytesIO(data))` → manipulate → `img.save(buf, format="PNG")` → `buf.getvalue()`.

**Crop coordinate system (from `image_sampler.py`):** `img.crop((left, top, right, bottom))` — PIL box tuple.

**Existing inline cropping in `visual_scorer.py:130`:** numpy array slicing `figma_blurred[y_start:y_end, :]` with `min(y_end, img_height)` clamping. `_MIN_SECTION_HEIGHT_PX = 8`.

## Test Landscape

**Pillow test patterns** — no mocking; all tests create real PIL Images in-memory:
- `_make_solid_png(w, h, rgb)` — `Image.new("RGB", (w, h), rgb)` + BytesIO
- `_make_split_png(w, h, top_rgb, bottom_rgb)` — pixel-level manipulation
- `_make_noisy_png(w, h, seed)` — numpy random + `Image.fromarray()`
- `TINY_PNG` base64 constant in `test_visual_diff.py`

**No conftest in `app/rendering/tests/` root** — each module is self-contained. Test helpers defined inline.

**Consumer test pattern (`test_visual_verify.py`):** section images as `dict[str, bytes]` keyed by node_id, `_FAKE_PNG = b"\x89PNG\r\n\x1a\n" + b"\x00" * 100`.

## Type Check Baseline

| Target | Tool | Errors | Warnings |
|--------|------|--------|----------|
| `app/rendering/` | pyright | 18 | 23 |
| `app/rendering/` | mypy | 1 (transitive) | 0 |
| `app/design_sync/visual_verify.py` | pyright | 0 | 0 |

## Files to Create

| File | What |
|------|------|
| `app/rendering/screenshot_crop.py` | `crop_section()` function |
| `app/rendering/tests/test_screenshot_crop.py` | 4 tests |

## Implementation Steps

### Step 1: Create `app/rendering/screenshot_crop.py`

```python
"""Section-level screenshot cropping utility."""

from __future__ import annotations

import io

from PIL import Image

from app.core.logging import get_logger

logger = get_logger(__name__)

_MIN_SECTION_HEIGHT_PX = 8


def crop_section(
    full_screenshot: bytes,
    y_offset: int,
    height: int,
    viewport_width: int,
) -> bytes:
    """Crop a full-page screenshot to a single section's bounds.

    Args:
        full_screenshot: Full-page PNG bytes (from Playwright).
        y_offset: Top edge of the section in pixels.
        height: Section height in pixels.
        viewport_width: Expected image width in pixels.

    Returns:
        Cropped PNG bytes. Falls back to full screenshot on error.
    """
    if height < _MIN_SECTION_HEIGHT_PX:
        logger.warning(
            "rendering.crop_skipped_small",
            y_offset=y_offset,
            height=height,
            min_height=_MIN_SECTION_HEIGHT_PX,
        )
        return full_screenshot

    try:
        img = Image.open(io.BytesIO(full_screenshot))
        img_width, img_height = img.size

        # Clamp bottom edge to image bounds
        y_end = min(y_offset + height, img_height)
        if y_end <= y_offset:
            logger.warning(
                "rendering.crop_out_of_bounds",
                y_offset=y_offset,
                height=height,
                img_height=img_height,
            )
            return full_screenshot

        cropped = img.crop((0, y_offset, min(viewport_width, img_width), y_end))
        buf = io.BytesIO()
        cropped.save(buf, format="PNG")
        logger.info(
            "rendering.section_cropped",
            y_offset=y_offset,
            height=height,
            cropped_height=y_end - y_offset,
        )
        return buf.getvalue()
    except Exception as exc:
        logger.warning("rendering.crop_failed", error=str(exc))
        return full_screenshot
```

### Step 2: Create `app/rendering/tests/test_screenshot_crop.py`

4 tests using real Pillow images (no mocks), following codebase convention:

| # | Test | Assertion |
|---|------|-----------|
| 1 | `test_crop_section_basic` | 680×2000 screenshot, crop y=500 h=300 → 680×300 PNG |
| 2 | `test_crop_section_clamps_to_image_height` | y=1900 h=300 on 2000px image → 680×100 PNG |
| 3 | `test_crop_section_small_height_returns_original` | h=4 (< `_MIN_SECTION_HEIGHT_PX`) → returns original bytes |
| 4 | `test_crop_section_invalid_bytes_returns_original` | Corrupt bytes → returns original without raising |

Helper: `_make_solid_png(w, h, rgb)` — inline, matches `test_image_sampler.py` pattern.

```python
"""Tests for section-level screenshot cropping."""

from __future__ import annotations

import io

import pytest
from PIL import Image

from app.rendering.screenshot_crop import crop_section


def _make_solid_png(w: int, h: int, rgb: tuple[int, int, int] = (128, 128, 128)) -> bytes:
    img = Image.new("RGB", (w, h), rgb)
    buf = io.BytesIO()
    img.save(buf, format="PNG")
    return buf.getvalue()


def _png_dimensions(data: bytes) -> tuple[int, int]:
    img = Image.open(io.BytesIO(data))
    return img.size  # (width, height)


class TestCropSection:
    def test_crop_section_basic(self) -> None:
        screenshot = _make_solid_png(680, 2000)
        result = crop_section(screenshot, y_offset=500, height=300, viewport_width=680)
        w, h = _png_dimensions(result)
        assert w == 680
        assert h == 300

    def test_crop_section_clamps_to_image_height(self) -> None:
        screenshot = _make_solid_png(680, 2000)
        result = crop_section(screenshot, y_offset=1900, height=300, viewport_width=680)
        w, h = _png_dimensions(result)
        assert w == 680
        assert h == 100  # clamped: 2000 - 1900

    def test_crop_section_small_height_returns_original(self) -> None:
        screenshot = _make_solid_png(680, 2000)
        result = crop_section(screenshot, y_offset=100, height=4, viewport_width=680)
        assert result == screenshot  # returned unchanged

    def test_crop_section_invalid_bytes_returns_original(self) -> None:
        bad_bytes = b"not-a-png"
        result = crop_section(bad_bytes, y_offset=0, height=100, viewport_width=680)
        assert result == bad_bytes  # graceful fallback
```

## Verification

- [ ] `uv run pytest app/rendering/tests/test_screenshot_crop.py -v` — 4 pass
- [ ] `uv run pyright app/rendering/screenshot_crop.py` — 0 errors
- [ ] `uv run mypy app/rendering/screenshot_crop.py` — 0 errors
- [ ] Pyright errors on `app/rendering/` ≤ 18 (baseline)
